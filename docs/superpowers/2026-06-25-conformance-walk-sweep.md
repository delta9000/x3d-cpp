---
title: Three-RAG Conformance Walk — Full Sweep (all 36 components)
date: 2026-06-25
method: docs/wiki/guides/conformance-walk.md
---

# Conformance Walk Sweep — 2026-06-25

Full per-component application of the [Three-RAG Conformance Walk](../wiki/guides/conformance-walk.md): one Sonnet agent per X3D component (36 total, 2 in parallel), each chaining `spec_rag` → `code_rag` → wiki cross-check → findings register, and reporting status + any **untracked** drift with `file:line` evidence.

**Run:** 36 agents · 1.87M tokens · 1,892 tool calls · ~77 min. Audited against `origin/main` (post-rebase, current docs).

## Headline numbers

- **Implementation:** 2 `ok` (Shape, Time) · 26 `partial` · 8 `na` (unimplemented)
- **Wiki accuracy:** 4 `ok` · 21 `partial` · 6 `fail` · 5 `na`
- **Tracked findings touched:** 43 open · 98 closed (closures spot-checked against code, not the doc's word)
- **NEW untracked findings surfaced: 91** — 17 high 🔴 · 35 medium 🟡 · 39 low ⚪

## The drift that matters most: false `Behaves ✓` claims

The campaign exists to catch the matrix asserting behavior the code does not ship. The walk found several, plus one silent runtime bug:

- 🔴 **CubeMapTexturing** — CubeMapTexturing extract gap: ComposedCubeMapTexture face textures silently dropped by MaterialSystem refOf()
- 🔴 **EnvironmentalEffects** — Fog 'Behaves ✓' is false — boundFog() has no production callers, no fog effect rendered
- 🔴 **EnvironmentalEffects** — TextureBackground 'Behaves ✓' is false — texture fields never read in production
- 🔴 **Geometry2D** — All 8 Geometry2D nodes absent from recognizedGeometryType() — extract silently drops them
- 🔴 **Geospatial** — GeoViewpoint.position silently reads as zero due to SFVec3d/SFVec3f type mismatch
- 🔴 **HAnim** — HAnim skin deformation unimplemented and untracked
- 🔴 **HAnim** — HAnimDisplacer morphing behavior entirely inert
- 🔴 **HAnim** — HAnimMotion animation driver entirely missing
- 🔴 **Layering** — LayerSet.order rendering suppression is inert
- 🔴 **Layering** — Layer.pickable flag is never consulted by PickSystem
- 🔴 **Layering** — Viewport.clipBoundary and Layer.viewport have no render-surface clipping implementation
- 🔴 **Layout** — ScreenFontStyle.pointSize silently ignored — font size always defaults to 1.0 when ScreenFontStyle is the Text node's fontStyle
- 🔴 **NURBS** — NURBS geometry nodes not in recognizedGeometryType() — all produce recognized=false silently
- 🔴 **NURBS** — NURBS interpolator set_fraction handlers are dead — value_changed never emitted
- 🔴 **ParticleSystems** — ParticleSystems: no behavioral runtime system — particle simulation entirely unimplemented
- 🔴 **Texturing3D** — ComposedTexture3D and ImageTexture3D have zero extraction logic — not populated into TextureRef
- 🔴 **VolumeRendering** — VolumeRendering component has zero behavioral runtime — all 13 nodes are inert stubs

## Status matrix

| Component | Spec | Impl | Wiki | Open | Closed | New |
|---|:--:|:--:|:--:|--:|--:|--:|
| CADGeometry | ✓ | ◑ | ◑ | 0 | 0 | 3 |
| Core | ✓ | ◑ | ◑ | 0 | 1 | 1 |
| CubeMapTexturing | ✓ | ◑ | ✗ | 0 | 0 | 4 |
| DIS | ✓ | — | — | 1 | 0 | 3 |
| EnvironmentalEffects | ✓ | ◑ | ◑ | 0 | 1 | 4 |
| EnvironmentalSensor | ✓ | ◑ | ◑ | 5 | 3 | 3 |
| EventUtilities | ✓ | ◑ | ✓ | 1 | 16 | 0 |
| Followers | ✓ | ◑ | ◑ | 1 | 7 | 2 |
| Geometry2D | ✓ | — | — | 0 | 0 | 4 |
| Geometry3D | ✓ | ◑ | ◑ | 1 | 0 | 1 |
| Geospatial | ✓ | ◑ | ◑ | 6 | 12 | 3 |
| Grouping | ✓ | ◑ | ◑ | 0 | 0 | 1 |
| HAnim | ✓ | ◑ | ✗ | 0 | 0 | 4 |
| Interpolation | ✓ | ◑ | ◑ | 0 | 3 | 2 |
| KeyDeviceSensor | ✓ | ◑ | ◑ | 1 | 9 | 1 |
| Layering | ✓ | ◑ | ✗ | 0 | 0 | 5 |
| Layout | ✓ | — | — | 0 | 0 | 5 |
| Lighting | ✓ | ◑ | ◑ | 0 | 0 | 3 |
| NURBS | ✓ | — | ✗ | 0 | 0 | 4 |
| Navigation | ✓ | ◑ | ◑ | 3 | 13 | 3 |
| Networking | ✓ | ◑ | ◑ | 8 | 0 | 2 |
| ParticleSystems | ✓ | — | — | 0 | 0 | 3 |
| Picking | ✓ | — | ◑ | 1 | 0 | 1 |
| PointingDeviceSensor | ✓ | ◑ | ◑ | 1 | 2 | 2 |
| Rendering | ✓ | ◑ | ◑ | 0 | 1 | 3 |
| RigidBodyPhysics | ✓ | ◑ | ◑ | 0 | 8 | 4 |
| Scripting | ✓ | ◑ | ✓ | 2 | 6 | 1 |
| Shaders | ✓ | ◑ | ◑ | 0 | 0 | 3 |
| Shape | ✓ | ✓ | ◑ | 0 | 9 | 2 |
| Sound | ✓ | ◑ | ✓ | 3 | 0 | 1 |
| Text | ✓ | ◑ | ◑ | 3 | 1 | 0 |
| TextureProjection | ✓ | — | — | 0 | 0 | 2 |
| Texturing | ✓ | ◑ | ✓ | 4 | 0 | 1 |
| Texturing3D | ✓ | ◑ | ✗ | 0 | 0 | 6 |
| Time | ✓ | ✓ | ◑ | 2 | 6 | 1 |
| VolumeRendering | ✓ | — | ✗ | 0 | 0 | 3 |

_Legend: ✓ ok · ◑ partial · ✗ fail/materially wrong · — n/a (unimplemented or no page)._

## Per-component detail

### CADGeometry

> IndexedQuadSet and QuadSet are fully wired and tested; CADPart TRS hierarchy works; but CADFace.shape type constraint is unenforced, CADAssembly/CADLayer lack behavioral tests, and the entire component is absent from the wiki coverage manifest.

- **Spec** ✓ — All 6 nodes fully specified in ISO 19775-1 §32. IndexedQuadSet/QuadSet at L1 (§32.4.5, §32.4.6): quad-to-triangle split rule, colorPerVertex always treated as TRUE, normalPerVertex-averaged normal generation when no Normal authored. CADPart at L2 (§32.4.4): full TRS transform hierarchy identical to Transform node. CADAssembly/CADFace/CADLayer at L2 (§32.4.1-3): grouping/product-structure hierarchy with name/visible/bboxCenter/bboxSize fields. CADFace.shape restricted to Shape|LOD|Transform (§32.4.2 prose). No ambiguity.
- **Impl** ◑ — IndexedQuadSet and QuadSet: fully wired — MeshBuilder.hpp:1643-1686 splits each consecutive 4 indices into two triangles (0,1,2)+(0,2,3); vertex-averaged normals when normalPerVertex=TRUE and no authored Normal (MeshBuilder.hpp:1778-1787, MSH-2 path); ragged tail ignored per spec; tested in mesh_builder_t2_test.cpp and mesh_builder_b7_test.cpp. CADPart: transform-bearing wiring live — TransformSystem.hpp:109 and SceneExtractor.hpp:501 apply the full TRS localMatrix; tested in transform_system_hanim_cadpart_test.cpp. CADAssembly and CADLayer: generated bindings exist (generated_cpp_bindings/CADAssembly.{hpp,cpp}, CADLayer.{hpp,cpp}); SceneExtractor traverses via generic SFNode/MFNode child loop (SceneExtractor.hpp:598-609); visible field honored generically (SceneExtractor.hpp:498-500); NO explicit runtime handling, NO behavioral test. CADFace: generated binding exists; shape SFNode slot traversed generically — spec constraint restricting shape to Shape|LOD|Transform is NOT enforced anywhere in code; no CADFace-specific test.
- **Wiki** ◑ — No dedicated subsystem page for CADGeometry exists in docs/wiki/subsystems/ (no cadgeometry.md or similar). IndexedQuadSet and QuadSet are mentioned by name in extract.md:122 under 'Composed/indexed sets (T1/T2)'. CADPart's role as a transform-bearing node is correctly documented in dirty-bounds-transform.md:93. CADAssembly, CADFace, and CADLayer appear nowhere in the wiki subsystem pages. coverage.md (docs/wiki/coverage.md) has no row for CADGeometry as a component — the component is entirely absent from the wiki coverage manifest.
- **Findings** — 0 open / 0 closed. docs/conformance/findings.yaml has zero entries tagged component: CADGeometry. The CADGeometry.md conformance matrix shows Exists=✓ for all 6 nodes and Extract=✓ for IndexedQuadSet and QuadSet only; Behaves and Findings columns are blank (—) for all 6 nodes.

  **New findings (3):**
  - 🟡 **CADFace.shape type constraint not enforced** — Spec §32.4.2 restricts CADFace.shape to [Shape|LOD|Transform]. SceneExtractor.hpp:598-609 uses a generic SFNode/MFNode child loop with no type guard for CADFace — any node type placed in the shape slot will be traversed. No enforcement found anywhere in runtime/. Contrast with Switch (SceneExtractor.hpp:570-578) and Collision.proxy (line 601) which both have explicit guards.
  - ⚪ **CADAssembly and CADLayer have no behavioral tests** — grep -rn 'CADAssembly|CADLayer' runtime/ returns only SceneExtractor.hpp:25 (a comment) and no test files. The conformance matrix CADGeometry.md shows Behaves=— for both. TransformSystem, BoundsSystem, SceneExtractor generic-child traversal are exercised through Transform/HAnim tests but never through CADAssembly or CADLayer fixtures.
  - ⚪ **CADGeometry component absent from wiki coverage manifest** — docs/wiki/coverage.md has no row for CADGeometry, IndexedQuadSet, QuadSet, CADPart, CADFace, CADAssembly, or CADLayer as a component entry. The extract.md:122 mention of IndexedQuadSet/QuadSet is incidental (part of the composed-sets list), not a component-level coverage claim. No subsystem page exists (ls docs/wiki/subsystems/ confirms no cadgeometry.md).

### Core

> All 9 Core nodes exist with correct generated bindings and PROTO-IS-001 is verified fixed, but there is no wiki subsystem page covering the Metadata* nodes or WorldInfo — those nodes are entirely undocumented in the living wiki.

- **Spec** ✓ — ISO/IEC 19775-1 §7 Core component is unambiguous. 9 nodes across L1 (MetadataBoolean/Double/Float/Integer/Set/String, WorldInfo, X3DStatement) and L2 (ProtoInstance). Metadata nodes share X3DMetadataObject interface (name, reference, value). WorldInfo is documentation-only — no behavioral effect. X3DStatement is a marker interface with no fields. ProtoInstance semantics governed by §4.4 / Table 4.4 IS access-type constraints. spec_rag.py confirms all node definitions.
- **Impl** ◑ — All 9 nodes exist as generated C++ bindings in generated_cpp_bindings/. MetadataBoolean (line 24): inherits X3DNode + X3DMetadataObject with MFBool _value — matches spec §7.4.1 exactly. WorldInfo (line 27): inherits X3DInfoNode (→X3DChildNode) with MFString _info and SFString _title — matches spec §7.4.7 exactly. X3DMetadataObject (line 21): carries name/reference fields, correct. X3DStatement (line 22): marker class, no fields, correct. ProtoInstance expansion: fully wired in runtime/X3DProtoExpand.hpp including nested IS-connection forwarding (lines 275-303) for PROTO-IS-001 fix. Behaves column is '—' for all 9 nodes in Core.md — no behavioral runtime system exercises these nodes beyond parse/extract (WorldInfo is documentation-only per spec; Metadata nodes have no behavioral semantics). No extract/behavioral gap for a documentation-only node set, but the column is intentionally empty, not a coverage hole.
- **Wiki** ◑ — proto-expand.md covers ProtoInstance/PROTO/IS-connection semantics thoroughly and accurately — file paths and class names verified current (runtime/X3DProtoExpand.hpp, X3DProtoClone.hpp, X3DProto.hpp). scene-graph.md mentions document metadata at line 22 (Head/meta struct). However, there is NO dedicated wiki subsystem page for the Core component's §7 nodes: MetadataBoolean/Double/Float/Integer/Set/String and WorldInfo are mentioned nowhere in docs/wiki/subsystems/. coverage.md has no row mapping to a Core-nodes page. This is a documentation gap — not a stale-claim problem, but a missing-page problem.
- **Findings** — 0 open / 1 closed. PROTO-IS-001 (status: fixed, severity: major): nested ProtoInstance MFNode overrides not propagating through IS/connect. Fix verified in runtime/X3DProtoExpand.hpp lines 268-303 — outer override forwarding loop iterates nested.isConnections, resolves outer ProtoField, calls resolveForwardedValue and setInstanceFieldValue before recursive expandInstance. Test coverage confirmed: proto_expand_audit_test.cpp:344 (nestedInstanceIsConnectForwardTest), proto_expand_test.cpp:261, xml_proto_capture_test.cpp. PROTO-IS-001 lacks an explicit 'component: Core' tag in findings.yaml — it is inferred from nodes: [ProtoInstance] — this is a minor schema inconsistency but not a conformance gap.

  **New findings (1):**
  - ⚪ **No wiki subsystem page for Core §7 nodes (Metadata*, WorldInfo)** — ls docs/wiki/subsystems/ | grep -i 'core|metadata|worldinfo' returns empty. coverage.md table (lines 36-79) has no row for a Core-nodes page. MetadataBoolean/Double/Float/Integer/Set/String and WorldInfo are referenced nowhere in docs/wiki/subsystems/*.md except a passing mention in scene-graph.md:22 about the Head/meta struct (which is a different concept — document-level <meta> vs. node-level MetadataBoolean).

### CubeMapTexturing

> CubeMapTexturing has generated bindings (parse/round-trip only) but zero runtime behavioral implementation — no extraction path, no wiki page, and no findings tracked for this gap.

- **Spec** ✓ — Spec is unambiguous. §34 (environmentalTexturing.md) defines three nodes: ComposedCubeMapTexture (L1, 9 fields including 6 face texture SFNodes), ImageCubeMapTexture (L2, 7 fields including url/load/autoRefresh from X3DUrlObject), GeneratedCubeMapTexture (L3, 5 fields including update SFString enum NONE|NEXT_FRAME_ONLY|ALWAYS and size SFInt32). §34.2 adds normative rules: three-dimensional (s,t,r) texture coordinates treated as a direction vector; repeatS/repeatT on face textures shall be ignored; GeneratedCubeMapTexture update='NEXT_FRAME_ONLY' must auto-reset to NONE on the following frame start with an output event.
- **Impl** ◑ — Generated bindings exist and are wired into the node factory (generated_cpp_bindings/ComposedCubeMapTexture.{hpp,cpp}, GeneratedCubeMapTexture.{hpp,cpp}, ImageCubeMapTexture.{hpp,cpp}; registered at X3DNodeFactory.cpp:312/350/369). Reflection tables are complete: all spec fields are present with correct types, access types, and defaults. Instantiation, parse round-trip, and default-value golden tests are covered by generated_cpp_bindings/test.cpp. However, there is ZERO runtime behavioral implementation: no entry in the extract layer (MaterialSystem.hpp refOf() dispatches on PixelTexture/MovieTexture/'long tail' but the long-tail branch reads only 'url' — ComposedCubeMapTexture's face textures are never read); no X3DEnvironmentTextureNode is dispatched or resolved in SceneExtractor, LightSystem, or TextureExtract; no RenderItem field carries cube-map descriptors; ShaderUniformVocabulary.hpp:155-158 reserves envDiffuse/envSpecular entries but they are documented-deferred (shaders.md §IBL). The conformance matrix (CubeMapTexturing.md) honestly marks Extract and Behaves as '--' for all three nodes.
- **Wiki** ✗ — No subsystem wiki page exists for the CubeMapTexturing component. Coverage.md (docs/wiki/coverage.md) has no row for this component — it is entirely absent from the manifest. The component is not mentioned in any of the 43 subsystem pages. The only adjacent coverage is in docs/wiki/subsystems/shaders.md which notes IBL/EnvironmentLight as deferred but does not mention CubeMapTexturing nodes or the extract-layer gap. docs/wiki/subsystems/extract-textures.md §refOf covers 'the long tail -> Url' without noting that ComposedCubeMapTexture face textures are silently dropped.
- **Findings** — 0 open / 0 closed. Zero findings in docs/conformance/findings.yaml are tagged component: CubeMapTexturing. The entire behavioral gap (no extraction, no face-texture dispatch, no GeneratedCubeMapTexture update semantics) is untracked.

  **New findings (4):**
  - 🔴 **CubeMapTexturing extract gap: ComposedCubeMapTexture face textures silently dropped by MaterialSystem refOf()** — runtime/extract/MaterialSystem.hpp:98-125 — refOf() dispatches on PixelTexture/MovieTexture then falls through to 'Url' branch reading only geombounds::getField<MFString>(*texNode, 'url', {}). ComposedCubeMapTexture has no 'url' field; its face textures (backTexture/frontTexture/etc.) are SFNode children. A ComposedCubeMapTexture in an Appearance.texture slot produces an empty TextureRef with source=Url and empty url[]. No code in the runtime reads the six face-texture SFNode fields.
  - 🟡 **No wiki subsystem page for CubeMapTexturing component; component absent from coverage.md manifest** — docs/wiki/coverage.md has no row with slug matching CubeMapTexturing, environmentalTexturing, or any variant. ls docs/wiki/subsystems/ returns no file matching cube or environment. The gap between existing bindings and the missing extraction path is entirely undocumented.
  - 🟡 **GeneratedCubeMapTexture 'update' auto-reset behavior (NEXT_FRAME_ONLY -> NONE + output event) has no runtime implementation** — Spec §34.4.2: 'at the start of the next frame, the update value shall be automatically set back to NONE to indicate that the rendering has already taken place. Since this is a change of value for the update field, an output event is automatically generated.' grep of runtime/ for GeneratedCubeMapTexture or update-reset logic returns zero hits. The binding (generated_cpp_bindings/GeneratedCubeMapTexture.hpp:130-140) stores and returns the enum but no system drives the reset or fires the output event.
  - ⚪ **ImageCubeMapTexture 'load' and autoRefresh fields are wired in reflection but the X3DUrlObject lifecycle (deferred load, auto-refresh timer) has no runtime driver for this node** — generated_cpp_bindings/ImageCubeMapTexture.cpp:87 and :139 expose 'load' and 'url' fields via reflection. X3DUrlObject::checkRangesAutoRefresh is called in validateRanges (ImageCubeMapTexture.cpp:252-260). However, grep of runtime/ for ImageCubeMapTexture or X3DUrlObject-driven load/refresh logic returns zero hits — no system actually fetches the DDS/cubemap URL or honours the load=FALSE flag.

### DIS

> All 6 DIS nodes exist only as generated field-reflection stubs; no DIS transport runtime exists, no wiki page has been created, and DISEntityManager/DISEntityTypeMapping behavioral gaps are untracked in findings.yaml.

- **Spec** ✓ — §28 (DIS component) is well-specified. L1 nodes (EspduTransform, ReceiverPdu, SignalPdu, TransmitterPdu) must react to networkMode (standAlone/networkReader/networkWriter), send/receive PDUs at readInterval/writeInterval, emit isActive/timestamp, and apply dead-reckoning (§28.2.3, §28.3.3–28.3.6). L2 nodes (DISEntityManager, DISEntityTypeMapping) must emit addedEntities/removedEntities MFNode events as entities arrive/depart the DIS network (§28.3.1–28.3.2). Spec is unambiguous.
- **Impl** — — All 6 nodes exist only as generated C++ field-reflection stubs in generated_cpp_bindings/ (e.g., generated_cpp_bindings/EspduTransform.cpp, generated_cpp_bindings/DISEntityManager.hpp). There is zero DIS-specific code in runtime/. The single runtime reference to EspduTransform is runtime/parse/tests/proto_expand_test.cpp:104, which uses the node only to exercise SFString→enum PROTO coercion — not any DIS behavior. No networkMode dispatch, no isActive emission, no dead-reckoning, no transport layer exists anywhere in the runtime tree.
- **Wiki** — — No subsystem page for DIS exists under docs/wiki/subsystems/. The DIS component is not listed in docs/wiki/coverage.md. A grep of the entire docs/wiki/ tree returns zero DIS or EspduTransform hits (excluding coverage.md). There is nothing to cross-check — the wiki is simply absent for this component.
- **Findings** — 1 open / 0 closed. One finding: NSN-10 (major/deferred) covers EspduTransform, ReceiverPdu, SignalPdu, TransmitterPdu for §28.2.3, 28.3 — networkMode dispatch + isActive/dead-reckoning behavior. Status is accurate; code confirms no behavior is implemented. DISEntityManager and DISEntityTypeMapping are not covered by any finding despite having unimplemented L2 behavioral requirements (addedEntities/removedEntities event emission). NSN-10 also lacks a component: DIS tag in findings.yaml, which may affect automated component-level filtering.

  **New findings (3):**
  - 🟡 **DISEntityManager/DISEntityTypeMapping L2 behavior untracked — no finding exists** — docs/conformance/components/DIS.md matrix rows for DISEntityManager and DISEntityTypeMapping show Behaves=— and Findings=— . NSN-10 in findings.yaml covers only [EspduTransform, ReceiverPdu, SignalPdu, TransmitterPdu]. The spec (§28.3.1–28.3.2) requires DISEntityManager to emit addedEntities/removedEntities MFNode events; generated_cpp_bindings/DISEntityManager.hpp:92,103 shows only stub getter/emitter with no runtime dispatch. This gap has no finding tracking it.
  - ⚪ **NSN-10 finding missing component: DIS tag** — docs/conformance/findings.yaml NSN-10 entry has no component: field; all other component-grouped findings use a component tag for automated matrix generation. Absence may cause DIS-level filtering to miss this finding in tooling.
  - ⚪ **No wiki subsystem page for DIS component** — ls docs/wiki/subsystems/ returns no dis.md or equivalent; grep of docs/wiki/coverage.md for DIS returns nothing. Per CLAUDE.md anti-drift discipline, a subsystem page should be created for every implemented (or stub-tracked) component. DIS has 6 nodes registered in the factory (generated_cpp_bindings/X3DNodeFactory.cpp:330-331) but no documentation.

### EnvironmentalEffects

> Fog and TextureBackground are incorrectly marked "Behaves ✓" in the conformance matrix — Fog binding is wired but boundFog() has zero production callers and no fog effect is ever rendered; TextureBackground texture fields are never read.

- **Spec** ✓ — All 5 nodes spec-documented: Background §24.4.1 (L1), Fog §24.4.2 (L2), FogCoordinate §24.4.3 (L4), LocalFog §24.4.4 (L4), TextureBackground §24.4.5 (L3). Normative requirements are clear: Background/Fog/TextureBackground are bindable; Fog applies linear/exponential depth-blend; FogCoordinate overrides per-vertex depth; LocalFog is a scoped, non-bindable fog; Background transparency is spec-required on both Background and TextureBackground.
- **Impl** ◑ — Background: binding works (BindingSystem.hpp:80-85 categories X3DBackgroundNode as 'Background'); skyColor/groundColor/skyAngle/groundAngle extracted in SceneExtractor.hpp:351-363 via BackgroundDesc; six URL faces read in cpu_raster/main.cpp:177-200. Gap: transparency field missing from BackgroundDesc (RenderItem.hpp:519-527 — struct has no transparency member despite §24.4.1 requiring it). Fog: binding registers the node on the stack (X3DExecutionContext.hpp:266 boundFog() declared) but boundFog() has ZERO production callers; no FogDesc struct exists anywhere; ShaderUniformVocabulary.hpp:167-175 references FogDesc.color/fogType/visibilityRange as dead vocabulary with no backing struct or extraction path; no fog rendering effect is produced. TextureBackground: binding recognized as 'Background' category; texture fields (backTexture, frontTexture, leftTexture, rightTexture, topTexture, bottomTexture) never read in any runtime or extraction code. FogCoordinate: generated bindings only (generated_cpp_bindings/FogCoordinate.hpp), no runtime behavior. LocalFog: generated bindings only (generated_cpp_bindings/LocalFog.hpp), no runtime behavior.
- **Wiki** ◑ — No dedicated EnvironmentalEffects subsystem page exists in docs/wiki/subsystems/ — no EnvironmentalEffects row in coverage.md. Background/Fog binding behavior is documented only as a side note in system-viewpointbind.md (accurate for binding semantics). The conformance matrix (docs/conformance/components/EnvironmentalEffects.md) marks Fog 'Behaves ✓' and TextureBackground 'Behaves ✓' — both are incorrect: Fog binding is wired but no fog effect reaches the renderer, and TextureBackground texture fields are never extracted. Background 'Behaves ✓' overstates: transparency is not surfaced in BackgroundDesc. BIND-06 is correctly noted as CLOSED with commit 95d1107, and removeNode() is present in BindingSystem.hpp:65 and surfaced as removeBoundNode() at X3DExecutionContext.hpp:263 (exercised in viewpoint_bind_test.cpp:126) — however commit 95d1107 is not resolvable in local git history (likely squash-merged).
- **Findings** — 0 open / 1 closed. One finding tagged via interfaces: [X3DBindableNode] affecting this component — BIND-06 (closed, commit 95d1107): deleted bound node doesn't behave as set_bind FALSE. The fix is present in code. No findings are tagged component: EnvironmentalEffects directly in findings.yaml.

  **New findings (4):**
  - 🔴 **Fog 'Behaves ✓' is false — boundFog() has no production callers, no fog effect rendered** — runtime/events/X3DExecutionContext.hpp:266 declares boundFog() but grep shows zero production callers outside the declaration itself. runtime/extract/ShaderUniformVocabulary.hpp:167-175 lists FogColor/FogType/FogRange/FogVisibilityRange uniform sources that reference 'FogDesc' — but no FogDesc struct exists anywhere in the codebase. SceneExtractor.hpp has no fog() readout method. The conformance matrix docs/conformance/components/EnvironmentalEffects.md marks Fog 'Behaves ✓' which is incorrect.
  - 🔴 **TextureBackground 'Behaves ✓' is false — texture fields never read in production** — grep for 'TextureBackground|backTexture|frontTexture|leftTexture|rightTexture|topTexture|bottomTexture' in runtime/ and examples/ (excluding generated_cpp_bindings and worktrees) returns zero results. TextureBackground only gets binding-stack recognition (BindingSystem.hpp:80-85 routes X3DBackgroundNode to 'Background' category). The conformance matrix marks it 'Behaves ✓' which is incorrect — it behaves identically to a plain Background with no texture faces, not per §24.4.5.
  - 🟡 **Background transparency field not surfaced in BackgroundDesc extraction descriptor** — Spec §24.4.1 and §24.4.5 require a transparency field on Background and TextureBackground. BackgroundDesc (RenderItem.hpp:519-527) contains only skyColor, skyAngle, groundColor, groundAngle, and backgroundChanged — no transparency member. SceneExtractor.hpp:351-363 background() method reads none of these transparency values. Neither cpu_raster nor poc_renderer reads 'transparency' from the bound Background node.
  - ⚪ **No dedicated EnvironmentalEffects subsystem wiki page; component coverage is invisible in coverage.md** — ls docs/wiki/subsystems/ shows no environmental-effects.md or similar. grep of coverage.md for 'EnvironmentalEffects|Background|TextureBackground|LocalFog|FogCoordinate' returns only one row (line 57) covering Viewpoint/NavigationInfo/Background/Fog binding under system-viewpointbind.md — extraction behavior, transparency, URL surfacing, and the four non-viewpoint nodes have no wiki coverage row.

### EnvironmentalSensor

> EnvironmentalSensor is substantially implemented (all three nodes wired, ENV-01 closed) but carries five open conformance gaps — most notably ENV-06 whose node scope in findings.yaml incorrectly omits TransformSensor despite the same missing-detach defect applying to it via the shared sensorActive_ map.

- **Spec** ✓ — All three nodes (ProximitySensor §22.4.1, TransformSensor §22.4.2, VisibilitySensor §22.4.3) have unambiguous normative requirements confirmed via spec_rag.py. Key rules verified: box-region enter/exit edge events (isActive/enterTime/exitTime), position_changed/orientation_changed while inside (Proximity + Transform), zero-volume box inertness, hierarchy-transform propagation, enabled gate, and dynamic insert/remove fire semantics. VisibilitySensor clarified as frustum-liberal (browser may err permissive on TRUE; must guarantee on FALSE). centerOfRotation_changed gated on LOOKAT nav mode.
- **Impl** ◑ — All three sensor nodes are enrolled and dispatched in ViewDependentSystem (runtime/scene/ViewDependentSystem.hpp:42-90). ProximitySensor (line 151): viewer-in-box test via world-to-local transform, isActive/enterTime/exitTime on edge, position_changed/orientation_changed with change-gate (ENV-04 closed). VisibilitySensor (line 189): cone (not frustum) test using fieldOfView half-angle widened by optional ViewVolume.aspect; fires isActive/enterTime/exitTime. TransformSensor (line 236): targetObject AABB vs sensor-local box AABB overlap, fires all required outputs with change-gate. Open gaps: (1) centerOfRotation_changed never emitted (ENV-03) — no code path exists anywhere in the runtime; (2) VisibilitySensor uses cone approximation not true 6-plane frustum (ENV-05); (3) No System::detach() hook — dynamic scene-graph removal of an active sensor silently orphans it without firing exitTime (ENV-06, applies to all three sensor types, not just two as tracked); (4) enter/exitTime use tick `now` not interpolated boundary-crossing time (ENV-08); (5) VisibilitySensor radius uses local unscaled box half-diagonal ignoring ancestor scale (ENV-09).
- **Wiki** ◑ — Two pages cover this component: docs/wiki/subsystems/system-viewdependent.md (updated 2026-06-22) and docs/wiki/subsystems/sensors.md (updated 2026-06-22). Both are substantially accurate. system-viewdependent.md correctly describes the ViewDependentSystem class, the cone-vs-frustum deferred seam, all three sensor update methods, the ViewVolume seam, and the 24 test functions (count verified). sensors.md line 224 contains a stale citation: 'docs/superpowers/BACKLOG.md — M2.5 (input seam + TouchSensor), M2D (drag sensors + KeyState), M2e (view-dependent sensors + LOD); all closed.' BACKLOG.md is deprecated since 2026-06-22 per project CLAUDE.md; it now only redirects to findings.yaml and the GitHub Project. The reference is harmless but points to a deprecated file. Neither wiki page mentions ENV-06's TransformSensor scope gap.
- **Findings** — 5 open / 3 closed. Open: ENV-03 (centerOfRotation_changed never emitted), ENV-05 (cone not frustum), ENV-06 (no detach hook — dynamic removal doesn't fire exit), ENV-08 (timestamp uses tick now not interpolated crossing time), ENV-09 (visibility radius ignores ancestor scale). Closed: ENV-01 (TransformSensor inert — fixed in commit 88fbd4d), ENV-04 (position/orientation_changed change-gate — code confirmed at ViewDependentSystem.hpp:167-174), ENV-07 (disable-deactivates edge — code confirmed via deactivateIfActive() at line 101-107). Note: ENV-04 and ENV-07 are marked commit:2b84a99 in findings.yaml but that hash does not resolve in the repo history (git cat-file returns fatal error); both fixes are present in the initial public release commit 330f00e. The commit reference is a stale/orphaned hash from pre-public history. ENV-02 (GeoProximitySensor) is deferred and belongs to the Geospatial component, not counted here.

  **New findings (3):**
  - 🟡 **ENV-06 scope omits TransformSensor: the no-detach gap applies to all three sensor types** — findings.yaml ENV-06 nodes: [ProximitySensor, VisibilitySensor] — TransformSensor is absent. ViewDependentSystem stores all three sensor types in the same sensorActive_ map (runtime/scene/ViewDependentSystem.hpp:46-47) with no detach/remove path. System base class (runtime/events/X3DSystem.hpp:35-55) has no detach() virtual method. Dynamic removal of an active TransformSensor node will orphan its entry in sensorActive_ and trSensorState_ just as Proximity/Visibility are orphaned — the spec (§22.4.2 last paragraph) mandates exitTime on removal from the hierarchy. ENV-06 should list all three nodes.
  - ⚪ **findings.yaml commit references for ENV-04 and ENV-07 (2b84a99) are orphaned hashes** — findings.yaml lines for ENV-04 and ENV-07 cite commit: 2b84a99. Running 'git cat-file -t 2b84a99' returns 'fatal: Not a valid object name 2b84a99' — the hash does not exist in the repository's public history. The actual fixes ARE present in the codebase (ViewDependentSystem.hpp:101-107 deactivateIfActive for ENV-07; line 167-174 change-gate for ENV-04) and were already in the initial public release commit 330f00e. The commit reference is a pre-squash/pre-rebase artifact and should be updated or removed to avoid confusion when bisecting.
  - ⚪ **sensors.md cites deprecated BACKLOG.md as a live reference** — docs/wiki/subsystems/sensors.md line 224: 'docs/superpowers/BACKLOG.md — M2.5 (input seam + TouchSensor), M2D (drag sensors + KeyState), M2e (view-dependent sensors + LOD); all closed.' BACKLOG.md was formally deprecated 2026-06-22 (the same date sensors.md was last updated); it now begins 'DEPRECATED — tracking moved to findings.yaml + GitHub Project.' The reference is in the 'Related specs and ADRs' section and should be replaced with a pointer to the relevant findings.yaml entries or removed.

### EventUtilities

> EventUtilities is fully implemented and documented with one known open gap: TRIG-4 (IntegerTrigger integerKey write does not emit integerKey_changed/triggerValue_changed per §30.4.6), deferred for spec re-review; all 16 other findings are closed and verified in code.

- **Spec** ✓ — All 7 nodes (BooleanFilter, BooleanSequencer, BooleanToggle, BooleanTrigger, IntegerSequencer, IntegerTrigger, TimeTrigger) are unambiguously defined in ISO/IEC 19775-1 §30. The one open question is TRIG-4: §30.4.6 specifies that writing integerKey (inputOutput) must emit both integerKey_changed and triggerValue_changed; the spec prose is clear ('Resetting the value of the integerKey field itself generates corresponding integerKey_changed and triggerValue_changed events'), so this is a known deferred gap, not a spec ambiguity.
- **Impl** ◑ — All 7 Systems are fully wired at runtime/events/EventUtilitySystem.hpp (lines 34-155). attachEventUtilities at runtime/events/X3DSceneBridge.hpp:383 registers all 7 and is called by attachFullRuntime (line 435). All 16 closed findings (TRIG-1/2/3/5/6, SEQ-1/2/3/4/5/7/8, EUF-1/2/4/5) are verified implemented in source — the handlers, stepwise-selection logic (sequencerStepIndex), and next/previous wrap-around all match spec. TRIG-4 remains unimplemented: there is no handler for write-side effects of integerKey (inputOutput) in IntegerTriggerSystem::attach — the closure at line 51-54 only handles set_boolean, not integerKey writes.
- **Wiki** ✓ — docs/wiki/subsystems/system-eventutility.md is accurate: all cited file paths exist and match (EventUtilitySystem.hpp, X3DSceneBridge.hpp, X3DExecutionContext.hpp, generated bindings, event_utility_test.cpp), spec clause references are correct (§30.4.1-§30.4.7), the interface signatures match the code, and TRIG-4 is correctly described as open. One minor stale cross-reference: the adjacent docs/wiki/subsystems/event-cascade.md line 153 cites 'docs/superpowers/BACKLOG.md rows RTC-5 and RTC-6' — BACKLOG.md is deprecated (2026-06-22) and no longer contains RTC-5/6 (they were closed in a spec-design doc). This affects event-cascade.md only, not the EventUtilities page itself.
- **Findings** — 1 open / 16 closed. 17 total findings in findings.yaml for EventUtilities (TRIG-1/2/3/4/5/6, SEQ-1/2/3/4/5/7/8, EUF-1/2/4/5). TRIG-4 is the sole open finding (major/open, §30.4.6 integerKey inputOutput write-side effects). All 16 closed findings were verified: code at runtime/events/EventUtilitySystem.hpp implements each behavior claimed closed at commit 47c0714. SEQ-6, EUF-3 were rejected (not tracked). FOL-*/Follower findings are a separate component.

### Followers

> Dampers and Chasers are fully wired and behaviorally conformant for non-overlapping transitions, but the wiki documents a ctest target (`x3d_event_followers`) that does not exist — the actual target is `x3d_events_tests` — meaning the documented test command runs zero tests.

- **Spec** ✓ — ISO/IEC 19775-1 §39 is well-understood. Spec RAG confirms node/field shapes for PositionChaser (§39.4.7) and PositionDamper (§39.4.9) match implementation. X3DChaserNode (§39.3.1) defines a re-basing FIR ramp; X3DDamperNode (§39.3.2) defines an order-cascade IIR with tolerance-based settle. The one open gap (FOL-5) — literal FIR overlapping-transition superposition — is correctly identified as a spec deviation and tracked as deferred.
- **Impl** ◑ — All 14 nodes wired via makeFollowerSystems()/attachFollowers(). DamperSystem (runtime/events/FollowerSystem.hpp:20-138): IIR cascade with α=exp(-dt/τ), convergence pre-check, passthrough for order==0/τ<=0, tolerance default 0.001, set_value snap, dual postEvent. ChaserSystem (FollowerSystem.hpp:148-233): re-basing lerp, startTime re-base on set_destination, exact end-of-transition at f>=1.0, SFRotation via slerpRotation in FollowerArith. MF element-wise handled via MFFollowerArith<Elem> (FollowerArith.hpp:44-70) with reshapeLike broadcast. attachFollowers called in both X3DSceneBridge.hpp:434 and tools/x3d-cli/sim_runtime.hpp:154. Partial: FOL-5 overlapping-transition FIR superposition (multiple set_destination within one duration window) is not implemented — deferred.
- **Wiki** ◑ — docs/wiki/subsystems/system-followers.md exists and is broadly accurate. Two concrete errors: (1) Line 68 documents the test command as `ctest --preset dev -R x3d_event_followers` but no such CTest target exists — CMakeLists.txt:1986 registers `add_test(NAME x3d_events_tests ...)` and all follower tests are compiled into that monolithic target; the `-R x3d_event_followers` regex matches nothing and runs zero tests. (2) Line 64 (and docs/conformance/components/Followers.md:25 and findings.yaml FOL-5 note line 746) all say 'see FOL-OVL in BACKLOG.md' but BACKLOG.md was deprecated 2026-06-22 and its own line 51 confirms FOL-OVL was migrated to FOL-5 in findings.yaml — the stale reference is to a deprecated file.
- **Findings** — 1 open / 7 closed. findings.yaml FOL-* entries: FOL-1 (fixed), FOL-2 (fixed), FOL-3 (fixed), FOL-4 (fixed), FOL-5 (deferred — overlapping Chaser FIR transitions), FOL-6 (fixed), FOL-7 (fixed), FOL-9 (fixed). Fixed findings verified against code: DamperSystem + ChaserSystem are real wired systems in runtime/events/FollowerSystem.hpp; attachFollowers scene-walk is real (X3DSceneBridge.hpp:368); MF element-wise arith is real (FollowerArith.hpp:44-70). FOL-5 deferred status is accurate — ChaserSystem implements only a single re-basing ramp, not the literal FIR superposition of overlapping set_destination events.

  **New findings (2):**
  - 🟡 **Wiki test command references non-existent CTest target** — docs/wiki/subsystems/system-followers.md:68 says `ctest --preset dev -R x3d_event_followers` but CMakeLists.txt:1986 registers `add_test(NAME x3d_events_tests ...)` — no target named `x3d_event_followers` exists; follower_conformance_test.cpp is compiled into x3d_events_tests (CMakeLists.txt:1974). The documented command runs zero tests.
  - ⚪ **FOL-5 note and two conformance docs reference deprecated BACKLOG.md for FOL-OVL** — docs/conformance/findings.yaml:746, docs/conformance/components/Followers.md:25, and docs/wiki/subsystems/system-followers.md:64 all say 'see FOL-OVL in BACKLOG.md'. BACKLOG.md:1 declares itself DEPRECATED (2026-06-22); BACKLOG.md:51 confirms FOL-OVL was remapped to FOL-5 in findings.yaml. The three stale references should point to FOL-5 in findings.yaml instead.

### Geometry2D

> All 8 Geometry2D nodes exist as generated bindings and are factory-registered, but none are wired into the extract pipeline (MeshBuilder, GeometryBounds) — shapes are parsed and silently dropped; no findings are tracked and no wiki page exists.

- **Spec** ✓ — All 8 nodes are unambiguously specified in X3D 4.1 §14. Arc2D (§14.3.1): line arc, startAngle/endAngle/radius. ArcClose2D (§14.3.2): PIE or CHORD filled arc, closureType/solid. Circle2D (§14.3.3): radius-only circle line. Disk2D (§14.3.4): filled disk with innerRadius/outerRadius/solid. Polyline2D (§14.3.5): 2D line segments via lineSegments MFVec2f. Polypoint2D (§14.3.6): 2D point set via point MFVec2f. Rectangle2D (§14.3.7): axis-aligned rect via size SFVec2f + solid. TriangleSet2D (§14.3.8): triangle set via vertices MFVec2f + solid. Texture mapping and solid field semantics are specified for applicable nodes.
- **Impl** — — Generated C++ bindings exist for all 8 nodes (generated_cpp_bindings/Arc2D.hpp, ArcClose2D.hpp, Circle2D.hpp, Disk2D.hpp, Polyline2D.hpp, Polypoint2D.hpp, Rectangle2D.hpp, TriangleSet2D.hpp) and all are factory-registered (generated_cpp_bindings/X3DNodeFactory.cpp:274-515). However, none appear in recognizedGeometryType() (runtime/extract/MeshBuilder.hpp:1239-1253), so they return recognized=false from buildLocalMesh and produce no triangle output. None appear in localGeometryBounds() (runtime/scene/GeometryBounds.hpp:52-140), so bounding boxes are always empty. Nodes parse and round-trip correctly but are completely inert at the extract layer — geometry is silently dropped.
- **Wiki** — — No subsystem page for Geometry2D exists under docs/wiki/subsystems/. The component is not listed in docs/wiki/coverage.md. The conformance matrix at docs/conformance/components/Geometry2D.md correctly marks Extract=✗ for all 8 nodes and Behaves=— (not assessed), so the matrix is not wrong, but there is no prose documentation of the gap, its rationale, or any planned tessellation approach.
- **Findings** — 0 open / 0 closed. Zero entries in docs/conformance/findings.yaml tagged component: Geometry2D. The full extract gap (all 8 nodes unimplemented) is entirely untracked — no open findings, no deferred items, no ADR. The conformance matrix records the Extract=✗ state but findings.yaml has no corresponding entry.

  **New findings (4):**
  - 🔴 **All 8 Geometry2D nodes absent from recognizedGeometryType() — extract silently drops them** — runtime/extract/MeshBuilder.hpp:1239-1252: recognizedGeometryType() lists no Geometry2D node; Arc2D, ArcClose2D, Circle2D, Disk2D, Polyline2D, Polypoint2D, Rectangle2D, TriangleSet2D all return recognized=false from buildLocalMesh, producing empty MeshData. Zero tessellation logic exists anywhere in the runtime for these nodes.
  - 🟡 **All 8 Geometry2D nodes absent from localGeometryBounds() — bounding boxes always empty** — runtime/scene/GeometryBounds.hpp:52-140: localGeometryBounds() handles Box, Sphere, Cone, Cylinder, ElevationGrid, GeoElevationGrid, Extrusion, Text, and generic coord-carrying nodes, but none of the Geometry2D nodes. None of the 2D nodes carry a 'coord' field (they use lineSegments, point, vertices, or implicit parameterization), so the generic coord fallback at line 74 also does not fire. Scenes containing Geometry2D geometry will have incorrect culling and picking.
  - ⚪ **No findings.yaml entries tracking the Geometry2D extract gap** — grep -n 'Geometry2D' docs/conformance/findings.yaml returns empty. The full extract gap for 8 nodes is untracked in the conformance tracker, leaving it invisible to the conformance gate and the GitHub project board.
  - ⚪ **No wiki subsystem page for Geometry2D — component undocumented** — ls docs/wiki/subsystems/ shows no geometry2d page. grep docs/wiki/coverage.md for 'geometry2d' returns empty. The component is absent from the living docs, meaning there is no documented rationale for the na implementation state, no tessellation design, and no upgrade path described.

### Geometry3D

> Geometry3D extraction is functional for 6/7 nodes but ElevationGrid silently drops authored color and normal nodes (EXT-001, open/major): MeshBuilder.hpp:1298-1326 never calls buildAttrs(), so colorPerVertex/normalPerVertex are inert for ElevationGrid and GeoElevationGrid.

- **Spec** ✓ — All 7 nodes (Box, Cone, Cylinder, Sphere §13.3.1-3/7 L1; IndexedFaceSet §13.3.6 L2; ElevationGrid §13.3.4 L3; Extrusion §13.3.5 L4) are fully defined in the spec RAG with normative field tables. Key rules: solid=culling, colorPerVertex/normalPerVertex for ElevationGrid and IFS, creaseAngle smoothing, Extrusion SCP-frame algorithm, per-face texture mapping per §13.3.x prose. No ambiguity for any tracked node.
- **Impl** ◑ — All 7 nodes are recognized and tessellated in runtime/extract/MeshBuilder.hpp::buildLocalMesh(). Box (line 1394), Sphere (1401), Cone (1407), Cylinder (1416): analytic parametric tessellation, solid read at 1284, default texcoords generated. Extrusion (line 1431): full SCP-frame sweep per §13.3.5 algorithm, beginCap/endCap, implicit TC3 texcoords. IndexedFaceSet (coord-fed path, line 1460+): full attribute resolution via buildAttrs() at line 1465 — Normal/Color/ColorRGBA/colorPerVertex/normalPerVertex/creaseAngle all wired. ElevationGrid (line 1298-1326): INERT for authored color + normal fields — the branch returns at line 1326 without calling buildAttrs(); emitHeightGrid() at line 833 pushes only flat generated normals and no colors. This is EXT-001 (open/major). texCoord is handled for ElevationGrid (lines 1320-1324). GeoElevationGrid has the same color/normal gap.
- **Wiki** ◑ — No dedicated subsystems/geometry3d.md page exists; the component is not listed in docs/wiki/coverage.md as a separate subsystem row. Coverage lives implicitly in docs/wiki/subsystems/extract.md (lines 122-128) which correctly enumerates all 7 recognized geometry types and their capabilities. The T3 attribute-resolution bullet (extract.md line 124) is structurally ambiguous — it does not explicitly exclude ElevationGrid — but is not an outright false claim since EXT-001 in findings.yaml already tracks the gap. The extract.md test table (lines 159-170) accurately lists x3d_mesh_builder_t2 as covering ElevationGrid with flat normals only, consistent with the code. No stale class names or missing file paths found.
- **Findings** — 1 open / 0 closed. EXT-001 (major, open): ElevationGrid/GeoElevationGrid drop authored color + normal nodes — confirmed by code at MeshBuilder.hpp:1298-1326 (no buildAttrs() call, emitHeightGrid() emits generated flat normals only). TXF-1 and TXF-2 reference Geometry3D nodes but are tagged component: Texturing, not Geometry3D, so not counted here.

  **New findings (1):**
  - ⚪ **No dedicated Geometry3D subsystem wiki page — component not tracked in coverage manifest** — docs/wiki/coverage.md has no row for a subsystems/geometry3d.md page. ls docs/wiki/subsystems/ confirms no geometry3d.md file exists. With 7 spec-defined nodes across 4 profile levels (L1–L4), a dedicated page would be the correct home for the §13 coverage matrix, EXT-001 remediation notes, and the ElevationGrid color/normal gap.

### Geospatial

> GeoViewpoint is silently broken for geodetic scenes: viewMatrix() reads the SFVec3d position field as SFVec3f, returning zero every tick — the bound-viewpoint binding machinery (BIND-01..09) is correct but the geo-coordinate is discarded before the camera matrix is formed.

- **Spec** ✓ — All 11 nodes and their normative field tables are unambiguous in ISO/IEC 19775-1v4 §25. Spec RAG retrieved full node declarations for GeoCoordinate (§25.3.1), GeoElevationGrid (§25.3.2), GeoPositionInterpolator (§25.3.7), GeoProximitySensor (§25.3.8), GeoViewpoint (§25.3.11). Key normative rule confirmed: GeoViewpoint.position and GeoCoordinate.point are SFVec3d/MFVec3d (double-precision geodetic), requiring geo-projection before use as Cartesian camera or vertex positions.
- **Impl** ◑ — GeoElevationGrid is the only node with real extraction wiring (MeshBuilder.hpp:1337-1388, flat-fallback + optional GeoProjection seam via MeshBuildOptions). GeoViewpoint binds and navigates via the BIND-01..09-fixed offset model (X3DExecutionContext.hpp:345-356), but viewMatrix() reads position as SFVec3f (line 350) while GeoViewpoint stores SFVec3d — any_cast fails silently, camera always placed at origin when GeoViewpoint is bound. GeoCoordinate.point is MFVec3d but MeshBuilder reads coord point as vector<SFVec3f> (line 1462) — any_cast fails, geometry using GeoCoordinate as coord node silently produces empty mesh. GeoPositionInterpolator has no System in InterpolatorRegistration.hpp (confirmed absent from makeInterpolatorSystems() list of 13 systems). GeoProximitySensor, GeoTouchSensor, GeoLOD, GeoLocation, GeoMetadata, GeoOrigin, GeoTransform exist only as generated bindings — no runtime System or special-cased extract path.
- **Wiki** ◑ — No dedicated Geospatial subsystem wiki page exists (docs/wiki/subsystems/ has no geo*.md). Coverage is fragmented: extract.md covers GeoElevationGrid/B5 and the GeoProjection seam accurately; system-navigation.md:60 correctly describes GeoViewpoint binding via reflection but does not disclose the SFVec3d→SFVec3f position type mismatch; seam-status.md lists GeoProjection as EXPERIMENTAL with flat-fallback. Nine of 11 nodes (GeoCoordinate, GeoLOD, GeoLocation, GeoMetadata, GeoOrigin, GeoTransform, GeoProximitySensor, GeoTouchSensor, GeoPositionInterpolator) have no wiki documentation at all. coverage.md has no Geospatial row.
- **Findings** — 6 open / 12 closed. Open/deferred (6): CONF-GEO (deferred — geo-projection seam prerequisite), TSN-1 (deferred — GeoTouchSensor not in PointingSensorSystem), TSN-2 (deferred — hitGeoCoord_changed unimplemented), ENV-02 (deferred — GeoProximitySensor no System), ENV-03 (open — centerOfRotation_changed not emitted), EXT-001 (open — color/normal nodes ignored on GeoElevationGrid). Closed (12): BIND-01..09 (GeoViewpoint navigation offset model, verified in code: X3DExecutionContext.hpp:345-356, NavigationSystem.hpp:74-76), INTERP-02 (empty-key guard, InterpolatorSystem.hpp:36-37), PIV-1 (attachInterpolators wiring, X3DSceneBridge.hpp:352), NAV-FLY-ROLL (documented pre-existing). Commits e3235ee/95d1107/2af9570/07c31ca predate public history (repo squash-merged at Initial public release) — verified by reading code behavior directly.

  **New findings (3):**
  - 🔴 **GeoViewpoint.position silently reads as zero due to SFVec3d/SFVec3f type mismatch** — runtime/events/X3DExecutionContext.hpp:350 calls getField<SFVec3f>(*vp, "position", {0,0,0}). GeoViewpoint stores _position as SFVec3d (generated_cpp_bindings/GeoViewpoint.hpp:182, 261). getField uses std::any_cast<SFVec3f> which throws on an SFVec3d std::any, returning the default {0,0,0}. Result: any scene with a bound GeoViewpoint has the camera pinned to world origin regardless of the authored position. Conformance matrix marks GeoViewpoint Behaves:✓ — that status is overstated; only the binding/offset mechanics are correct, not the geodetic position.
  - 🟡 **GeoCoordinate as coord node silently produces empty mesh** — runtime/extract/MeshBuilder.hpp:1462 reads getField<std::vector<SFVec3f>>(*coord, "point", {}). GeoCoordinate._point is MFVec3d (generated_cpp_bindings/GeoCoordinate.hpp:118,177). The any_cast fails, returning empty vector, so any IndexedFaceSet/PointSet/IndexedLineSet using a GeoCoordinate coord node yields zero triangles with no error. Not a separate tracked finding; implied by CONF-GEO but the exact drop mechanism is unrecorded.
  - ⚪ **No Geospatial subsystem wiki page; coverage.md missing Geospatial row** — ls docs/wiki/subsystems/ shows no geo*.md. grep of docs/wiki/coverage.md finds no Geospatial row. Nine of 11 component nodes (GeoCoordinate, GeoLOD, GeoLocation, GeoMetadata, GeoOrigin, GeoTransform, GeoProximitySensor, GeoTouchSensor, GeoPositionInterpolator) are undocumented in the wiki. Existing coverage is split across extract.md:123, system-navigation.md:60, seam-status.md GeoProjection row.

### Grouping

> Transform and Switch are fully wired; Group/StaticGroup addChildren/removeChildren events are structurally declared but never functionally connected — the children list is never mutated at runtime when these inputOnly events arrive.

- **Spec** ✓ — All 4 nodes (Group L1, StaticGroup L3, Switch L2, Transform L1) are unambiguously specified. Transform TRS matrix formula P'=T*C*R*SR*S*-SR*-C*P is normative (§10.4.4). Switch whichChoice semantics (−1/OOB = draw nothing; all nodes receive events regardless of whichChoice) are clear (§10.4.3). addChildren/removeChildren semantics for X3DGroupingNode are defined in §10.2.1. StaticGroup no-mutation guarantee specified in §10.4.2.
- **Impl** ◑ — Transform: fully wired. TransformSystem::localMatrix (runtime/scene/TransformSystem.hpp:96) reads translation/rotation/scale/center/scaleOrientation via reflection and computes the exact normative 5-factor matrix; world transforms propagated incrementally via TransformSystem::propagate. Switch: fully wired. SceneExtractor::walk (runtime/extract/SceneExtractor.hpp:570-578) special-cases nodeTypeName()==Switch, reads whichChoice via reflection, recurses only children[whichChoice] or nothing if <0/OOB. Group pass-through: wired. DirtyChildren from a 'children' MFNode write triggers SceneExtractor::rewalkSubtree (SceneExtractor.hpp:446), correctly producing added/removed deltas. addChildren/removeChildren: INERT. X3DGroupingNode::onAddChildren (generated_cpp_bindings/X3DGroupingNode.hpp:103) dispatches to _onAddChildrenHandler, but no runtime system ever calls setOnAddChildrenHandler — grep of runtime/ confirms zero hits. The event cascade marks DirtyChildren on delivery (X3DExecutionContext.hpp:385-390) but the children MFNode is never actually mutated, so the subtree re-walk re-emits identical children. StaticGroup: no behavioral enforcement of the no-modification constraint; treated as a generic pass-through grouper in SceneExtractor (SceneExtractor.hpp:595-610). visible=FALSE culling is wired (SceneExtractor.hpp:497-500). bboxSize author-hint override is wired in BoundsSystem.
- **Wiki** ◑ — No dedicated Grouping subsystem wiki page exists. Coverage maps to docs/wiki/subsystems/dirty-bounds-transform.md (Transform hierarchy, world-transform propagation, BoundsSystem) and docs/wiki/subsystems/extract.md (Switch/LOD DFS special-cases). Both pages are accurate and their file paths/class names verify against the current code. extract.md line 172 mentions 'Switch/LOD special-cases' in the test table but gives no further detail on Switch semantics. Neither page documents the addChildren/removeChildren inert-handler gap. coverage.md has no Grouping-specific row — the component is covered implicitly across multiple pages with no explicit mapping noted.
- **Findings** — 0 open / 0 closed. No findings.yaml entries are tagged to the Grouping component (Group, Switch, StaticGroup, Transform). The Wave-4 comment at findings.yaml:772-779 states 'Switch audited clean (whichChoice handled in the extractor)' but does not file a finding for addChildren/removeChildren. COL-1/COL-2/COL-3 (Collision node, §23.4.2) and LOD-1 (closed) are the only close-adjacent findings and are not Grouping nodes. Zero open, zero closed findings formally tagged to Grouping.

  **New findings (1):**
  - 🟡 **addChildren/removeChildren events are inert: children list never mutated at runtime** — generated_cpp_bindings/X3DGroupingNode.hpp:103 — onAddChildren() dispatches to _onAddChildrenHandler which is null by default. grep -rn 'setOnAddChildrenHandler|setOnRemoveChildrenHandler' runtime/ returns zero hits. X3DExecutionContext.hpp:385-390 marks DirtyChildren on addChildren/removeChildren delivery but SceneExtractor::rewalkSubtree (SceneExtractor.hpp:446-486) re-walks the unchanged children list, producing no net change. Spec §10.2.1 requires addChildren to insert new nodes into the children list and removeChildren to remove matching nodes. This is untracked — not in findings.yaml.

### HAnim

> All 6 HAnim nodes exist as generated bindings and HAnimHumanoid/Joint participate correctly in the TRS transform hierarchy, but all behavioral semantics — skinning, displacement morphing, and motion animation — are entirely unimplemented and untracked in findings.yaml.

- **Spec** ✓ — HAnim §26 (ISO 19775-1 §26) is unambiguous: 6 nodes confirmed via spec_rag.py. HAnimHumanoid (§26.3.2, 38 fields) and HAnimJoint (§26.3.3, 28 fields) carry full TRS frames. Skin deformation is normative (skinCoord/skinNormal/skinCoordIndex/skinCoordWeight). HAnimDisplacer (§26.3.1) specifies coordIndex+displacements morphing. HAnimMotion (§26.3.4, L2) specifies channels/values/frameIndex animation with output events cycleTime/elapsedTime/frameCount. HAnimSegment and HAnimSite are grouping nodes at L1. Spec is well-understood and unambiguous.
- **Impl** ◑ — WIRED: (1) All 6 nodes registered in generated_cpp_bindings/X3DNodeFactory.cpp:363-368 and parse/instantiation works. (2) HAnimHumanoid and HAnimJoint are explicitly treated as transform-bearing nodes (TransformSystem.hpp:109 isTransform()), with TRS world-matrix computation and incremental propagation. Tested in runtime/scene/tests/transform_system_hanim_cadpart_test.cpp. (3) SceneExtractor.hpp:501 uses isTransform() generically so HAnimHumanoid/Joint carry correct world transforms in the render extract. (4) SceneExtractor generic DFS (SceneExtractor.hpp:598-610) recurses ALL node-typed fields, so Shape children of HAnimSegment/HAnimSite are passively emitted. INERT/MISSING: (1) Skin deformation: PackedMesh.hpp:54-55,120 explicitly marks Joints/Weights attributes as Phase 2+ TODO; no code reads skinCoord/skinNormal/skinCoordIndex/skinCoordWeight to deform geometry. (2) Displacement morphing: no code in runtime/ reads HAnimDisplacer.coordIndex or HAnimDisplacer.displacements. (3) Motion animation: HAnimMotion fields (channels, values, frameDuration, frameIndex, loop, etc.) exist in bindings but no system drives them or emits cycleTime/elapsedTime/frameCount output events.
- **Wiki** ✗ — No subsystem wiki page exists for HAnim (docs/wiki/subsystems/ has no hanim.md). No row in docs/wiki/coverage.md for HAnim. The conformance matrix at docs/conformance/components/HAnim.md is auto-generated and correctly shows all Extract/Behaves/Findings as '—', so it is not wrong, but the missing subsystem page and coverage.md entry means the component is invisible in the wiki navigation.
- **Findings** — 0 open / 0 closed. grep of docs/conformance/findings.yaml for HAnim, HAnimHumanoid, HAnimJoint, HAnimSegment, HAnimSite, HAnimDisplacer, HAnimMotion returns zero matches. All behavioral gaps — skinning, displacement, motion animation — are entirely untracked.

  **New findings (4):**
  - 🔴 **HAnim skin deformation unimplemented and untracked** — runtime/extract/PackedMesh.hpp:54-55 marks VertexAttrib::Joints and VertexAttrib::Weights as 'Phase 2+'; PackedMesh.hpp:120 has explicit TODO comment. No code in runtime/ reads HAnimHumanoid.skinCoord/skinNormal or HAnimJoint.skinCoordIndex/skinCoordWeight to produce deformed geometry. Not tracked in docs/conformance/findings.yaml.
  - 🔴 **HAnimDisplacer morphing behavior entirely inert** — generated_cpp_bindings/HAnimDisplacer.hpp exists with coordIndex/displacements/weight fields but grep of runtime/ (excluding tests/vendor) for 'displace' or 'HAnimDisplacer' returns no runtime behavioral code. ISO §26.3.1 requires applications to use coordIndex+displacements to identify and offset vertex groups. Not tracked in docs/conformance/findings.yaml.
  - 🔴 **HAnimMotion animation driver entirely missing** — generated_cpp_bindings/HAnimMotion.hpp has all fields (channels, values, frameIndex, frameDuration, loop, enabled, startFrame, endFrame) but grep of runtime/ for HAnimMotion returns zero hits outside generated bindings. No system drives frame advancement or emits cycleTime/elapsedTime/frameCount output events per ISO §26.3.4. Not tracked in docs/conformance/findings.yaml.
  - ⚪ **No HAnim subsystem wiki page or coverage.md row** — ls docs/wiki/subsystems/ shows no hanim.md. grep of docs/wiki/coverage.md for HAnim returns no output. The component is invisible in the wiki and the coverage table, violating the project anti-drift discipline (CLAUDE.md: 'add it to mkdocs.yml nav + a docs/wiki/coverage.md row').

### Interpolation

> All 13 interpolator nodes are functionally wired and both conformance test suites pass, but SquadOrientationInterpolator.normalizeVelocity (a declared spec field) is silently ignored by the implementation and undisclosed in the wiki.

- **Spec** ✓ — All 13 nodes audited via spec_rag.py. §19.3.1 (X3DInterpolatorNode empty-key rule), §19.2.4 (Hermite spline), §19.4.4 (EaseInEaseOut 10-step algorithm), §19.4.13 (Squad). Node signatures and field types match spec declarations exactly. EaseInEaseOut correctly extends X3DChildNode (not X3DInterpolatorNode), so the §19.3.1 empty-key rule does not apply to it. SquadOrientationInterpolator.normalizeVelocity is a real spec field (§19.2.3 + §19.4.13) even though Squad has no explicit velocity vectors — behavior when TRUE is underspecified in prose.
- **Impl** ◑ — All 13 systems wired in makeInterpolatorSystems() (InterpolatorRegistration.hpp:35-61). Linear family: InterpolatorSystem<>/MultiInterpolatorSystem<> with correct lerp delegates (lerpf, lerpVec2/3, lerpColorHsv, slerpRotation, slerpNormal) — all 8 nodes fully wired. Spline family: hermiteSpline<T> in SplineInterpolation.hpp:67 handles closed/keyVelocity/normalizeVelocity — wired for SplinePositionInterpolator, SplinePositionInterpolator2D, SplineScalarInterpolator. EaseInEaseOut: easeInEaseOut() in SplineInterpolation.hpp:238 implements the full 10-step §19.4.4 algorithm with global-domain remapping. INTERP-02 empty-key guard present in InterpolatorSystem.hpp:37,64 and SplineInterpolatorSystem.hpp:33,50. GAP: SquadOrientationInterpolatorSystem (SplineInterpolatorSystem.hpp:44-57) calls squadOrientation(key, keyValue, fraction) — the function signature at SplineInterpolation.hpp:199-201 takes no normalizeVelocity parameter and the field is silently ignored. This field is present in the generated binding (generated_cpp_bindings/SquadOrientationInterpolator.hpp:94) but never read at event time.
- **Wiki** ◑ — docs/wiki/subsystems/system-interpolators.md accurately describes file roles, class templates, seam points, spec annotations, and test commands. All cited file paths exist. DRIFT: The findings (Interpolation.md line 23-26 and findings.yaml lines 164-168, 314-317) reference commit 07c31ca as the closing commit for INTERP-01, INTERP-02, and PIV-1. This SHA does not exist in the git history (confirmed: git cat-file -t 07c31ca → fatal). The code ships the correct behavior (SplineInterpolation.hpp and SplineInterpolatorSystem.hpp present since initial release 330f00e), so the closure is factually correct but the commit reference is a dangling SHA — likely a pre-publication squash artifact. The wiki at system-interpolators.md:130 says SquadOrientationInterpolator 'N=2 reduces to plain SLERP' but omits that normalizeVelocity is declared in spec and silently ignored by the implementation.
- **Findings** — 0 open / 3 closed. INTERP-01 (critical/closed): Spline/Squad/EaseInEaseOut systems absent — fixed, code ships correctly. INTERP-02 (major/closed): empty-key guard — verified at InterpolatorSystem.hpp:37,64 and SplineInterpolatorSystem.hpp:33,50. PIV-1 (minor/closed): attachInterpolators production wiring — verified at X3DSceneBridge.hpp:352,433. All 3 closed findings confirmed by running ctest --preset dev -R 'x3d_event_interpolators|x3d_interpolator_conformance' — both tests pass. Closing commit 07c31ca cited in all three findings does not exist in the repo history; the code was present in the initial public release at 330f00e.

  **New findings (2):**
  - ⚪ **SquadOrientationInterpolator.normalizeVelocity silently ignored** — Spec §19.4.13 + §19.2.3 declare normalizeVelocity (SFBool, inputOutput, default FALSE) on SquadOrientationInterpolator. Generated binding at generated_cpp_bindings/SquadOrientationInterpolator.hpp:94 exposes getNormalizeVelocity(). SquadOrientationInterpolatorSystem::attach() at SplineInterpolatorSystem.hpp:49-56 calls squadOrientation(interp->getKey(), interp->getKeyValue(), fraction) — the function at SplineInterpolation.hpp:199-201 has no normalizeVelocity parameter and the field is never read. Wiki at system-interpolators.md:130 does not disclose this gap. Not tracked in findings.yaml.
  - ⚪ **Closing commit 07c31ca (INTERP-01, INTERP-02, PIV-1) is a dangling SHA** — findings.yaml lines 166, 175, 316 and Interpolation.md lines 23-26 all cite commit: 07c31ca as the closing commit for all three Interpolation findings. git cat-file -t 07c31ca → fatal: Not a valid object name. The underlying code (SplineInterpolation.hpp, SplineInterpolatorSystem.hpp) is present since the initial public release at 330f00e. The closure itself is factually valid, but the commit reference is unsatisfiable and will permanently fail any tooling that tries to verify it.

### KeyDeviceSensor

> KeyDeviceSensor is substantially implemented and wired (9/10 findings closed), but StringSensor deletionAllowed has a latent UTF-8 multi-byte deletion bug (KeyDeviceSensorSystem.hpp:103 pop_back()) not yet tracked.

- **Spec** ✓ — §21.4.1 (KeySensor) and §21.4.2 (StringSensor) are fully specified. All output fields (keyPress, keyRelease, actionKeyPress, actionKeyRelease, shiftKey, controlKey, altKey, isActive for KeySensor; enteredText, finalText, isActive, deletionAllowed for StringSensor) have unambiguous normative rules. §21.2 exclusive-focus arbitration (KDS-6, the single open finding) is also clearly specified: enabling one sensor must send enabled=FALSE to all others.
- **Impl** ◑ — KeyDeviceSensorSystem (runtime/events/KeyDeviceSensorSystem.hpp) is fully wired: attach() registers both node types, update() drains KeyState::events each tick, driveKeySensor() posts all 8 KeySensor outputs (keyPress, keyRelease, actionKeyPress, actionKeyRelease, shiftKey, controlKey, altKey, isActive) and driveStringSensor() posts all 3 StringSensor outputs (enteredText, finalText, isActive) with correct §4.4.8.3 coalescing. Production wiring via attachKeyDeviceSensors (runtime/events/X3DSceneBridge.hpp:408) is confirmed. The only defect: st.text.pop_back() at KeyDeviceSensorSystem.hpp:103 removes one byte, not one UTF-8 codepoint — deletion of multi-byte characters (non-ASCII) will corrupt the enteredText accumulator. KDS-6 (exclusive-focus arbitration) is not implemented.
- **Wiki** ◑ — docs/wiki/subsystems/system-keydevice.md is broadly accurate: all cited file paths exist (KeyDeviceSensorSystem.hpp, KeyState.hpp, X3DSceneBridge.hpp, generated_cpp_bindings/KeySensor.hpp, generated_cpp_bindings/StringSensor.hpp, runtime/events/tests/key_device_sensor_test.cpp, runtime/events/tests/key_state_test.cpp), class/method names match code, seam API matches X3DExecutionContext, and test coverage claims match the test file. Minor issue: line 125 references docs/superpowers/BACKLOG.md for 'wave-4 context'; the file exists and is marked deprecated, so citing it as historical context is acceptable per project policy, but the doc does not note the BACKLOG.md deprecation. No materially wrong claims found.
- **Findings** — 1 open / 9 closed. 10 total KDS findings (KDS-1 through KDS-10). KDS-6 is the sole OPEN finding (§21.2 exclusive-focus arbitration, minor). KDS-1..5, KDS-7..10 are all CLOSED at commit 85b90b0 and verified: KeyDeviceSensorSystem is live code shipping all the behaviors those findings tracked.

  **New findings (1):**
  - 🟡 **StringSensor.deletionAllowed uses pop_back() — breaks multi-byte UTF-8 deletion** — runtime/events/KeyDeviceSensorSystem.hpp:103 calls st.text.pop_back() which removes one byte. A UTF-8 character may be 2-4 bytes; deleting a non-ASCII character will leave a truncated/invalid byte sequence in enteredText. Tests at runtime/events/tests/key_device_sensor_test.cpp:155-168 only exercise ASCII ('a','b') — the bug is undetected. §21.4.2 requires enteredText to be a valid UTF-8 string.

### Layering

> All three Layering nodes exist as generated bindings and are parseable, but no runtime system interprets LayerSet.order, LayerSet.activeLayer, Layer.pickable, or Viewport.clipBoundary — and the component has no wiki page and zero conformance findings.

- **Spec** ✓ — ISO 19775-1 §35 is unambiguous: LayerSet.order controls render sequence and unlisted layers are suppressed; LayerSet.activeLayer designates the navigation layer; Layer.visible gates rendering; Layer.pickable gates pick traversal (even when invisible); Layer.viewport constrains output to a sub-region; Viewport.clipBoundary [left/right/bottom/top] specifies fractions of the render surface. All three nodes (Layer, LayerSet, Viewport) are at L1. spec_rag.py node Layer/LayerSet/Viewport confirmed full field grammars from layering.md §35.3.1–35.4.3.
- **Impl** ◑ — All three nodes exist as generated C++ bindings with complete field reflection tables. generated_cpp_bindings/Layer.cpp:75-187 shows objectType/pickable/viewport/visible all in the FieldTable (inherited via X3DLayerNode). generated_cpp_bindings/Viewport.hpp has clipBoundary with range validation [0,1]. HOWEVER: no runtime system wires any Layering semantics. SceneExtractor.hpp:168-179 (fullSnapshot) walks scene_.rootNodes directly with no LayerSet traversal; SceneExtractor.hpp:598-610 (generic field loop) recurses ALL children of all nodes ignoring LayerSet.order suppression; SceneExtractor.hpp:497-500 does honor visible=FALSE generically, so Layer.visible=FALSE incidentally stops rendering — but this is the generic visible field, not Layer-specific logic. PickSystem.hpp:60-63 (build) takes scene.rootNodes with no Layer.pickable awareness. LayerSet.activeLayer, LayerSet.order, Layer.pickable, and Viewport.clipBoundary are inert: stored but never consumed by any runtime system.
- **Wiki** ✗ — No wiki subsystem page exists for Layering. docs/wiki/coverage.md lists 41 subsystems — none maps to Layering or these nodes. `ls docs/wiki/subsystems/` has no layering.md or layer.md. No ADR covers the Layering design stance. The component is entirely absent from the wiki.
- **Findings** — 0 open / 0 closed. Zero entries in docs/conformance/findings.yaml reference Layer, LayerSet, or Viewport nodes. grep for 'component: Layering' and 'nodes:.*Layer' both return nothing. The conformance model (docs/conformance/model.json) marks all three nodes as exists=true, extracts=n/a, behaves=n/a with empty findings arrays — consistent with zero findings.yaml entries but also confirming no behavioral expectations are tracked.

  **New findings (5):**
  - 🔴 **LayerSet.order rendering suppression is inert** — runtime/extract/SceneExtractor.hpp:598-610 — the generic field loop recurses ALL SFNode/MFNode children unconditionally. LayerSet.order is never read; layers absent from the order list are rendered anyway (violates §35.4.2: 'Layers included in the layers field that are not listed in the order field are not rendered').
  - 🔴 **Layer.pickable flag is never consulted by PickSystem** — runtime/scene/PickSystem.hpp:60-63 — PickSystem::build() populates roots_ from scene.rootNodes with no Layer awareness. No code path reads Layer.pickable or X3DLayerNode::getPickable(). §35.3.1 requires: 'An X3DLayerNode node specified with pickable set to FALSE does not participate in picking operations.'
  - 🔴 **Viewport.clipBoundary and Layer.viewport have no render-surface clipping implementation** — generated_cpp_bindings/Viewport.hpp:79 — clipBoundary is stored and range-validated, but no runtime system reads it. runtime/extract/SceneExtractor.hpp contains no reference to 'Viewport', 'clipBoundary', or viewport sub-region logic. §35.4.3 requires output to be clipped to the specified [left/right/bottom/top] fractions.
  - 🟡 **LayerSet.activeLayer (navigation layer designation) is inert** — generated_cpp_bindings/LayerSet.hpp:75 — getActiveLayer()/setActiveLayer() exist, but runtime/events/ contains no reference to LayerSet or activeLayer. §35.4.2 requires activeLayer to designate the layer in which navigation takes place; NavigationSystem (runtime/events/NavigationSystem.hpp) has no such coupling.
  - ⚪ **Layering component has no wiki page and no coverage.md row** — docs/wiki/coverage.md lists 41 subsystems with zero entries for Layering, Layer, LayerSet, or Viewport. ls docs/wiki/subsystems/ confirms no layering.md page exists. The component has no documented design stance, no behavioral coverage matrix, and no ADR for the inert-fields decision.

### Layout

> All five Layout nodes exist only as generated data-container bindings with no runtime geometry, transform, or pixel-scale logic wired; ScreenFontStyle's pointSize field is silently ignored in TextExtract, and the component has no wiki page and zero conformance findings.

- **Spec** ✓ — ISO 19775-1 §36 is unambiguous. Layout (L1) defines 7 fields (align, offset, offsetUnits, scaleMode, size, sizeUnits) for 2D rectangle placement and content scaling. LayoutGroup and LayoutLayer carry layout+viewport SFNode slots. ScreenFontStyle (L2) replaces FontStyle's 'size' with 'pointSize' (in points). ScreenGroup (L2) scales so 1 unit = 1 pixel. All normative rules confirmed via spec_rag.py.
- **Impl** — — All five nodes (Layout, LayoutGroup, LayoutLayer, ScreenFontStyle, ScreenGroup) exist solely as generated C++ data-container bindings in generated_cpp_bindings/. No runtime system reads Layout.align/offset/size/scaleMode to compute a 2D region, no LayoutLayer/LayoutGroup produce a pixel-coordinate viewport, and ScreenGroup performs no pixel-scale transform. The SceneExtractor (runtime/extract/SceneExtractor.hpp:568-611) treats LayoutGroup and LayoutLayer as generic pass-through grouping nodes via the blind SFNode/MFNode field loop — correct only for child traversal, not layout geometry. ScreenFontStyle is additionally broken at the text-extract seam: readFontStyleParams (runtime/extract/TextExtract.hpp:133) reads field 'size' with default 1.0; ScreenFontStyle has no 'size' field (only 'pointSize'), so font size is always forced to 1.0 when a ScreenFontStyle node is used.
- **Wiki** — — No subsystem wiki page exists for the Layout component. docs/wiki/coverage.md has no Layout row. The only mentions in the wiki are incidental: templates.md cites LayoutGroup as a test fixture for the C++ generator, and decisions/0004-virtual-mixins.md uses LayoutGroup as a diamond-inheritance example. The extract-text.md page (docs/wiki/subsystems/extract-text.md) covers text+layout via FontMetrics but does not mention ScreenFontStyle or the broken pointSize gap.
- **Findings** — 0 open / 0 closed. Zero findings in docs/conformance/findings.yaml tagged component: Layout or referencing any Layout-component node (Layout, LayoutGroup, LayoutLayer, ScreenFontStyle, ScreenGroup). The component is entirely untracked in the conformance system.

  **New findings (5):**
  - 🔴 **ScreenFontStyle.pointSize silently ignored — font size always defaults to 1.0 when ScreenFontStyle is the Text node's fontStyle** — runtime/extract/TextExtract.hpp:133 calls geombounds::getField<float>(*fsNode, "size", 1.0f). ScreenFontStyle has no 'size' field — only 'pointSize' (generated_cpp_bindings/ScreenFontStyle.hpp:213). The getField call returns the default 1.0 unconditionally, so any scene using ScreenFontStyle as a fontStyle child renders text at size 1 regardless of the authored pointSize value. No nodeTypeName branch in readFontStyleParams handles this case.
  - 🟡 **Layout component has no conformance findings — entire component untracked in findings.yaml** — grep -n 'component:' docs/conformance/findings.yaml | grep -i layout returns nothing. None of the 5 Layout nodes appear in any finding. The unimplemented 2D layout rectangle geometry, pixel-coordinate viewport, and ScreenGroup pixel-scale transform are not tracked.
  - ⚪ **No wiki subsystem page for Layout component — coverage.md has no Layout row** — ls docs/wiki/subsystems/ | grep -i layout returns nothing. grep -i layout docs/wiki/coverage.md returns only an incidental 'layout' mention in the extract-text.md row description. The component is undocumented as a subsystem.
  - 🟡 **ScreenGroup pixel-scale transform not implemented — passes through as plain grouping node** — Spec §36.4.5: ScreenGroup shall scale so 1 unit = 1 pixel in both directions. SceneExtractor.hpp:595-611 handles ScreenGroup via the generic SFNode/MFNode pass-through loop with no pixel-scale transform injection. No runtime system computes or applies this scale.
  - 🟡 **Layout 2D rectangle geometry entirely unimplemented — LayoutLayer/LayoutGroup treated as plain grouping** — Spec §36.4.1–36.4.3: Layout node fields (align, offset, offsetUnits, scaleMode, size, sizeUnits) must determine a 2D region and content scale. SceneExtractor.hpp:595-611 falls through to the generic MFNode loop for both LayoutGroup and LayoutLayer, reading no Layout node fields. No 2D region is computed or applied anywhere in runtime/.

### Lighting

> Lighting extraction is structurally sound but shadows/shadowIntensity are silently dropped from LightDesc, SpotLight beam fallback defaults are transposed (beamWidth↔cutOffAngle), and the wiki incorrectly claims SpotLight test coverage that does not exist.

- **Spec** ✓ — ISO X3D 4.1 §17.4.1–17.4.3 is well-understood. DirectionalLight (L1), PointLight (L2), SpotLight (L2) are fully specified. All field types, defaults, and constraints are unambiguous. X3DLightNode (§17.3.1) adds shadows (SFBool, default FALSE) and shadowIntensity (SFFloat, default 1) to every light type.
- **Impl** ◑ — runtime/extract/LightSystem.hpp + RenderItem.hpp::LightDesc implement the core extraction path: all three light types recognized (LightSystem.hpp:70-77); on==false skip correct (LightSystem.hpp:94); color/intensity/ambientIntensity/global read correctly (LightSystem.hpp:126-134); world-frame direction/location resolved via per-path Mat4 re-accumulation (LightSystem.hpp:138-148); attenuation/radius for Point/Spot correct; beamWidth/cutOffAngle extracted for Spot (LightSystem.hpp:151-154). Gaps: (1) shadows and shadowIntensity are spec-defined fields on X3DLightNode but are absent from LightDesc (RenderItem.hpp:473-491) and never read by LightSystem — a consumer cannot know whether a light casts shadows; (2) LightSystem.hpp:152 has beamWidth fallback default 1.5708f (π/2) but spec §17.4.3 specifies π*3/16 ≈ 0.589049 — the fallback is wrong (latent: only fires if geombounds field-get fails, e.g. PROTO-expanded or dynamically built nodes); (3) LightSystem.hpp:153 has cutOffAngle fallback 0.7854f (π/4) but spec says π/2 ≈ 1.570796 — again wrong fallback; beamWidth and cutOffAngle defaults are effectively swapped from spec.
- **Wiki** ◑ — docs/wiki/subsystems/extract-textures.md is the sole covering page (no dedicated Lighting page; coverage.md maps LightSystem.hpp to extract-textures.md). The page accurately describes LightSystem::collect, the global flag fidelity, scopeRoot semantics, world-frame direction/location resolution, and the per-path transform re-accumulation. One material drift: extract-textures.md:117 claims the x3d_light_system ctest covers 'DirectionalLight/PointLight/SpotLight field reads', but the actual test file (runtime/extract/tests/light_system_test.cpp) contains no SpotLight test case and zero hits for SpotLight, beamWidth, or cutOffAngle. The page also makes no mention of shadows/shadowIntensity being absent from LightDesc.
- **Findings** — 0 open / 0 closed. No findings.yaml entries tagged component: Lighting. The shadows/shadowIntensity omission and SpotLight fallback defaults are untracked.

  **New findings (3):**
  - 🟡 **shadows and shadowIntensity absent from LightDesc — spec-required fields not surfaced** — Spec §17.3.1 X3DLightNode defines shadows (SFBool, default FALSE) and shadowIntensity (SFFloat, default 1) on every light type. LightDesc (runtime/extract/RenderItem.hpp:473-491) has no shadows or shadowIntensity member. LightSystem::makeLight (runtime/extract/LightSystem.hpp:120-156) never reads either field. A rendering consumer therefore cannot implement shadow casting from the extraction descriptor. Not tracked in docs/conformance/findings.yaml.
  - 🟡 **SpotLight beamWidth and cutOffAngle fallback defaults are wrong — values appear swapped** — LightSystem.hpp:152 uses beamWidth fallback 1.5708f (π/2); spec §17.4.3 says π*3/16 ≈ 0.589049. LightSystem.hpp:153 uses cutOffAngle fallback 0.7854f (π/4); spec says π/2 ≈ 1.570796. Generated binding SpotLight.hpp:385 correctly initializes _beamWidth{0.589049} and SpotLight.hpp:391 _cutOffAngle{1.570796}. The LightSystem fallbacks are the inverse of each other — they appear transposed. The fallback is latent (fires only when geombounds cannot read the field) but would yield a fully-open cone with zero-width hot spot, the opposite of spec behavior.
  - ⚪ **Wiki claims SpotLight field reads tested — no SpotLight test case exists** — docs/wiki/subsystems/extract-textures.md:117 states the x3d_light_system ctest covers 'DirectionalLight/PointLight/SpotLight field reads'. grep of runtime/extract/tests/light_system_test.cpp for 'SpotLight', 'beamWidth', 'cutOffAngle' returns zero hits. The test file has four test blocks: PointLight world-location, DirectionalLight global default, on==false skip, and PointLight field reads — no SpotLight block. The doc claim is inaccurate.

### NURBS

> All 13 NURBS nodes exist as schema-complete generated bindings but zero behavior is wired: no geometry node is in MeshBuilder's recognized set, and the three NURBS interpolators have dead handler slots never registered by the event system — the component is entirely stub-only with no findings tracked.

- **Spec** ✓ — All 13 nodes are specified in ISO 19775-1 §27 (component NURBS, levels 1-4). Key nodes verified via spec_rag.py: NurbsCurve §27.4.3, NurbsPatchSurface §27.4.6, NurbsOrientationInterpolator §27.4.5. Spec requirements are unambiguous: geometry nodes must tessellate parametric surfaces; interpolator nodes must compute positions/orientations/surface-params on set_fraction and emit value_changed.
- **Impl** — — All 13 nodes are present as generated C++ bindings in generated_cpp_bindings/ (e.g. NurbsCurve.cpp, NurbsPatchSurface.cpp, NurbsOrientationInterpolator.cpp) with correct field tables and field types. However: (1) MeshBuilder.hpp:1239-1252 — recognizedGeometryType() lists no NURBS node; NurbsCurve, NurbsPatchSurface, NurbsSweptSurface, NurbsSwungSurface, NurbsTrimmedSurface all return recognized=false and empty MeshData. (2) InterpolatorRegistration.hpp:35-61 — makeInterpolatorSystems() registers 13 interpolator systems, none are NURBS; NurbsOrientationInterpolator, NurbsPositionInterpolator, NurbsSurfaceInterpolator have _onSet_fractionHandler slots (NurbsOrientationInterpolator.hpp:278) that are never set by any runtime code, so set_fraction events are silently dropped and value_changed is never emitted. (3) No NURBS symbol appears anywhere under runtime/ except tests and parse (which only exercise parse round-trip, not behavior). The Behaves column in docs/conformance/components/NURBS.md is entirely — for all 13 rows, accurately reflecting zero behavioral implementation.
- **Wiki** ✗ — No subsystem page exists for NURBS under docs/wiki/subsystems/ — there are 41 subsystem pages and none covers the NURBS component. No row for NURBS appears in docs/wiki/coverage.md (confirmed: grep returns no hits). The only wiki mentions are: docs/wiki/subsystems/cli-suite.md:101 (accurate — notes NURBS is unsupported/counted), docs/wiki/decisions/0012-no-external-math-lib.md:29+47 (lists NURBS as future Eigen scope), docs/wiki/decisions/0002-event-api-for-inputonly.md:61 (notes NURBS nodes carry many inputOnly handler slots). The component has no dedicated subsystem page and is not registered in the coverage manifest, violating the docs-as-part-of-diff discipline.
- **Findings** — 0 open / 0 closed. Zero entries in docs/conformance/findings.yaml with component: NURBS or any of the 13 NURBS node names. The behavioral gap — all 13 nodes are inert — is not formally tracked in findings.yaml. The NURBS.md conformance matrix exists and shows the correct Extract=✗ and Behaves=— status, but the absence of findings means there is no open tracking item for the geometry tessellation gap or the dead interpolator handlers.

  **New findings (4):**
  - 🟡 **No NURBS subsystem wiki page and no coverage.md row** — ls docs/wiki/subsystems/ — no nurbs.md or equivalent exists among 41 pages. grep -i nurbs docs/wiki/coverage.md returns no output. CLAUDE.md requires a subsystem page and coverage.md row for every subsystem; NURBS is a 13-node ISO component with zero page coverage.
  - 🔴 **NURBS geometry nodes not in recognizedGeometryType() — all produce recognized=false silently** — runtime/extract/MeshBuilder.hpp:1239-1252 — recognizedGeometryType() enumerates every supported geometry type; NurbsCurve, NurbsPatchSurface, NurbsSweptSurface, NurbsSwungSurface, NurbsTrimmedSurface are absent. The external_geom_seam_test.cpp:~60 confirms NurbsPatchSurface yields recognized=false. Geometry extraction is fully inert with no findings.yaml tracking.
  - 🔴 **NURBS interpolator set_fraction handlers are dead — value_changed never emitted** — NurbsOrientationInterpolator.hpp:158-161 — onSet_fraction() is a no-op unless _onSet_fractionHandler is set. InterpolatorRegistration.hpp:35-61 — makeInterpolatorSystems() registers 13 systems; NurbsOrientationInterpolator, NurbsPositionInterpolator, NurbsSurfaceInterpolator are absent. No other runtime/ file registers these handlers. Events routed to set_fraction are silently dropped; value_changed output is never produced.
  - 🟡 **NURBS component behavioral gap (13 nodes) untracked in findings.yaml** — grep -rn 'NURBS\|nurbs' docs/conformance/findings.yaml returns zero hits. The gap in tessellation behavior and interpolator behavior for a full ISO component is not recorded as open findings, making it invisible to the conformance gate and audit trail.

### Navigation

> Navigation core (EXAMINE/FLY/LOOKAT, all BIND rules, LOD, Billboard, Collision proxy) is well-implemented and verified; the only material gap is avatar-volume collision (COL-1/COL-3/CONF-NAV-COLLISION, all tracked as deferred), plus the untracked inert viewAll field.

- **Spec** ✓ — All 7 nodes (Billboard, Collision, LOD, NavigationInfo, OrthoViewpoint, Viewpoint, ViewpointGroup) have clear normative prose in §23.4.x. Key rules verified via spec_rag: Viewpoint/NavigationInfo binding stacks (§23.3.1, §7.2.2), mode semantics EXAMINE/FLY/LOOKAT/NONE/WALK (§23.4.4), LOD level-select step function + child-count clamp (§23.4.3), Billboard axis-rotation and viewer-alignment algorithms (§23.4.1), Collision proxy suppression and avatar-volume detection (§23.4.2), viewAll field behavior (§23.4.5/23.4.6). No ambiguity in the spec for these nodes.
- **Impl** ◑ — Wired: EXAMINE/FLY/LOOKAT/NONE navigation modes (runtime/events/NavigationSystem.hpp); all BIND-01..09 offset/jump/retainUserOffsets/transition rules (runtime/events/ViewpointBindSystem.hpp); Billboard rotation via billboardLocalMatrix called at SceneExtractor.hpp:502-506; LOD level_changed with child-count clamp at ViewDependentSystem.hpp:75-83; Collision.proxy suppressed from render output at SceneExtractor.hpp:600-601. Not wired: avatar-volume collision detection for Collision.isActive/collideTime (COL-1, CONF-NAV-COLLISION, COL-3 — no avatar-sphere/WALK collision system exists); viewAll field (§23.4.6) exists in generated bindings (generated_cpp_bindings/X3DViewpointNode.hpp:220) but NavigationSystem has no handling; ViewpointGroup has no runtime attach/behavior anywhere in runtime/.
- **Wiki** ◑ — docs/wiki/subsystems/system-navigation.md and system-viewpointbind.md are accurate: file paths exist, class names match, test counts match (navigation_test has 10 cases as documented; viewpointbind tests all present). One stale citation: system-viewpointbind.md:223 references docs/superpowers/BACKLOG.md ('CONF-VIEWNAV Phase 2 conformance findings BIND-01..BIND-08') — that file is deprecated as of 2026-06-22 per CLAUDE.md; those findings have moved to docs/conformance/findings.yaml. No dedicated page exists for Billboard, LOD, Collision, or ViewpointGroup as Navigation nodes (covered partially under system-viewdependent.md for Billboard/LOD). Coverage.md rows for system-navigation.md and system-viewpointbind.md are present and accurate.
- **Findings** — 3 open / 13 closed. Open/deferred: CONF-NAV-COLLISION (major, deferred — avatar-volume collision subsystem not built), COL-1 (critical, deferred — Collision.isActive/collideTime never fire), COL-3 (major, deferred — Collision.enabled=FALSE subtree propagation unobservable without collision subsystem). All 13 closed findings verified in code: BIND-01..09 confirmed in ViewpointBindSystem.hpp/ViewpointOffset.hpp; COL-2 confirmed at SceneExtractor.hpp:600-601; LOD-1 confirmed at ViewDependentSystem.hpp:75-83; NAV-LOOKAT-SCALE and NAV-FLY-ROLL confirmed in NavigationSystem.hpp + navigation_test.cpp cases 6-10. Note: BIND-*/COL-*/LOD-*/NAV-* findings in findings.yaml lack a component: Navigation tag; only CONF-NAV-COLLISION carries one. All 16 are effectively Navigation-scoped.

  **New findings (3):**
  - ⚪ **viewAll field (§23.4.6 / §23.4.5) is inert with no tracking entry** — viewAll exists in generated_cpp_bindings/X3DViewpointNode.hpp:220 (getViewAll/setViewAll) but grep of runtime/ finds zero handling in NavigationSystem.hpp or any other system. Spec §23.4.6 requires: when viewAll is set TRUE or a viewpoint is bound with viewAll=TRUE, the view must be modified to encompass the entire scene's bounding box. docs/superpowers/specs/2026-06-18-conformance-gap-register.md:139 names 'Viewpoint.viewAll' as a known gap but no findings.yaml entry exists.
  - ⚪ **system-viewpointbind.md cites deprecated BACKLOG.md** — docs/wiki/subsystems/system-viewpointbind.md:223 references 'docs/superpowers/BACKLOG.md — CONF-VIEWNAV Phase 2 conformance findings BIND-01..BIND-08'. BACKLOG.md line 1 declares 'DEPRECATED (2026-06-22)'; CLAUDE.md deprecation notice says deferrals now live in findings.yaml and the GitHub Project. The BIND-01..08 findings are in findings.yaml and the stale link should be replaced with a pointer to findings.yaml.
  - ⚪ **BIND-*/COL-*/LOD-*/NAV-* findings.yaml entries lack component: Navigation tag** — python3 audit of findings.yaml shows only CONF-NAV-COLLISION carries component: Navigation. BIND-01 through BIND-09, COL-1 through COL-3, LOD-1, NAV-LOOKAT-SCALE, NAV-FLY-ROLL all have no component field, so component-scoped queries return only 1 finding instead of 16. This breaks tooling that filters by component tag (e.g., the generated Navigation.md derives its list from a different source than the yaml component field).

### Networking

> Inline is fully implemented at parse-time; Anchor has no behavioral wiring (embedder-scope in headless SDK); LoadSensor is a data-only stub with all 8 behavioral findings still open/deferred.

- **Spec** ✓ — All three L2/L3 nodes are unambiguous in ISO 19775-1: Anchor §9.4.1 (grouping + URL navigation on user activation), Inline §9.4.2 (sub-scene embedding, load field, DEF isolation, self-reference prohibition), LoadSensor §9.4.3 (isActive/isLoaded/loadTime/progress/timeOut events on child X3DUrlObject load state). Note: spec_rag.py node LoadSensor misfires — returns shaders.md §31.2.3.4 instead of networking.md §9.4.3; the query variant returns the correct normative source. Spec requirements are well-understood; the issue is exclusively implementation depth.
- **Impl** ◑ — Inline: fully wired — runtime/InlineExpand.hpp:123 expandInlines() handles load=TRUE, DEF isolation (child defs never merged into scene.defs per §9.4.2), route hoisting, and writer round-trip via Scene::expandedInlines. load=FALSE nodes are correctly left un-expanded (documented as Tier-3 deferred). Anchor: generated binding at generated_cpp_bindings/Anchor.hpp:29 stores parameter field and inherits X3DGroupingNode+X3DUrlObject, but zero runtime files implement URL-navigation on user activation — PointingSensorSystem.hpp has no Anchor handling. In a headless renderer-agnostic SDK this is an embedder callback responsibility, consistent with the matrix showing Behaves=—. LoadSensor: generated binding at generated_cpp_bindings/LoadSensor.hpp:28 provides all fields (children, isLoaded, loadTime, progress, timeOut) with emit* methods, but there is zero runtime behavioral implementation — no LoadSensorSystem class, no per-tick observation loop, no event emission. Confirmed: grep of runtime/ for LoadSensor returns no hits.
- **Wiki** ◑ — docs/wiki/subsystems/inline-expand.md accurately documents Inline expansion: file paths, class names, test commands, and spec refs (§9.4.2) all verified correct against the code. docs/wiki/subsystems/system-asset-io.md correctly identifies NSN-1..9 as 'now wireable' after ADR-0023 but does not claim they are shipped. docs/wiki/coverage.md line 49 maps inline-expand.md to InlineExpand.hpp and X3DImportExport.hpp — accurate. Gap: no wiki subsystem page covers Anchor URL-activation behavioral semantics or LoadSensor. The Networking component has no dedicated subsystem page; inline-expand.md covers only the Inline node's parse-time path. This is an undocumented scope boundary, not a factual error.
- **Findings** — 8 open / 0 closed. 8 open/deferred NSN findings in findings.yaml (NSN-1 through NSN-9, NSN-8 audited and rejected per line 773 comment '26 audited → 25 confirmed (rejected NSN-8 1/3)'). All 8 remaining findings verified still applicable: NSN-1 (LoadSensor not wired as System), NSN-2 (isActive not emitted), NSN-3 (isLoaded not emitted), NSN-4 (loadTime not emitted), NSN-5 (progress not emitted), NSN-6 (timeOut deadline unimplemented), NSN-7 (url/load change reset unimplemented), NSN-9 (already-resolved children burst not emitted). Code confirms none are resolved — generated_cpp_bindings/LoadSensor.hpp has emit* stubs but runtime/InlineExpand.hpp and all other runtime files contain no LoadSensor tick logic. NSN-1 note correctly states 'Unblocked by asset-resolver/IO seam (shipped 2026-06-23)' — the seam exists but LoadSensor has not yet been wired to use it.

  **New findings (2):**
  - ⚪ **No wiki coverage of Anchor URL-navigation behavioral scope boundary** — grep of docs/wiki/subsystems/ for 'Anchor' returns zero hits (only system-asset-io.md mentions it in passing as an X3DUrlObject consumer). The conformance matrix shows Anchor Behaves=— with no wiki page explaining why (headless SDK: URL navigation is embedder callback responsibility, not SDK behavior). The PointingSensorSystem.hpp has no Anchor handling — zero search results for 'Anchor' in that file. This is an undocumented scope decision, not a bug, but creates audit opacity for future readers.
  - ⚪ **spec_rag.py node LoadSensor returns wrong spec section (shaders.md §31.2.3.4 instead of networking.md §9.4.3)** — Running 'uv run --no-sync python scripts/spec_rag.py node LoadSensor' returns '# LoadSensor [X3D 4.1] ... prose: shaders.md §31.2.3.4 LoadSensor'. The normative networking prose at §9.4.3 is only reachable via the query variant. This is a RAG index/lookup bug that would mislead any walk using the node subcommand for LoadSensor.

### ParticleSystems

> ParticleSystems has zero behavioral implementation — only generated data-layer bindings exist, the entire particle simulation engine is absent, there is no wiki page, and the gap is completely untracked in findings.yaml.

- **Spec** ✓ — ISO §40 (X3D 4.1) normative requirements are well-understood: 10 nodes across Levels 1-2, particle lifecycle simulation loop (spawn/integrate/expire), emitter geometry (ConeEmitter, ExplosionEmitter, PointEmitter, PolylineEmitter, SurfaceEmitter, VolumeEmitter), physics models (ForcePhysicsModel, WindPhysicsModel, BoundedPhysicsModel), color/texCoord ramps with keyed interpolation, isActive outputOnly lifecycle event, enabled/createParticles semantics per §40.4.5. Spec prose is unambiguous.
- **Impl** — — All 10 nodes exist only as generated data-layer bindings under generated_cpp_bindings/ (ParticleSystem.hpp/cpp, ConeEmitter.hpp/cpp, ForcePhysicsModel.hpp/cpp, etc.). There is no runtime behavioral system: no ParticleSystem system class anywhere under runtime/, no attachment in tools/x3d-cli/sim_runtime.hpp, no particle simulation loop, no emitter position sampling, no physics force integration, no color ramp interpolation, no isActive event emission. The nodes parse and serialize but exhibit zero normative behavior. Evidence: grep of runtime/ for 'particle' or 'Particle' returns empty; code_rag.py query 'particle system' --all returns only generated_cpp_bindings hits.
- **Wiki** — — No wiki subsystem page exists for ParticleSystems. The component does not appear anywhere in docs/wiki/coverage.md — not as a covered row, not as a planned row. docs/wiki/subsystems/ contains no particle-related page. The component is entirely absent from the wiki target set.
- **Findings** — 0 open / 0 closed. Zero entries in docs/conformance/findings.yaml tagged to ParticleSystems. The complete absence of behavioral implementation is not tracked anywhere in the conformance register.

  **New findings (3):**
  - 🔴 **ParticleSystems: no behavioral runtime system — particle simulation entirely unimplemented** — grep of runtime/ for 'particle' returns zero hits. No ParticleSystem system class exists. sim_runtime.hpp (tools/x3d-cli/sim_runtime.hpp) does not attach any particle system. All 10 nodes (ParticleSystem, ConeEmitter, ExplosionEmitter, PointEmitter, PolylineEmitter, SurfaceEmitter, VolumeEmitter, ForcePhysicsModel, WindPhysicsModel, BoundedPhysicsModel) are data-only generated bindings. No particle lifecycle, position integration, emitter sampling, physics force application, color ramp interpolation, or isActive event emission is implemented anywhere in the codebase.
  - 🟡 **ParticleSystems: no wiki subsystem page and missing from coverage.md** — docs/wiki/subsystems/ has no particle page. grep of docs/wiki/coverage.md for 'particle' returns empty. The component has no row (covered or planned) in the coverage manifest, violating the anti-drift discipline that requires every functional area to have a page.
  - 🟡 **ParticleSystems: zero findings registered despite fully unimplemented component** — docs/conformance/findings.yaml has no entries for component ParticleSystems (grep returns empty). The conformance register does not reflect that §40 behavioral simulation is entirely absent, leaving auditors no signal about this gap.

### Picking

> All five Picking-component nodes (Line/Point/Primitive/VolumePickSensor, PickableGroup) exist only as generated data bindings with no runtime System — the pick engine ships but no Picking-component consumer has been written, matching the open deferred finding CONF-PICKSENSOR.

- **Spec** ✓ — ISO/IEC 19775-1 §38 is well-covered by spec_rag.py. Key normative rules confirmed: pick sensors evaluate pickingGeometry vs pickTarget between rendered frames; isActive=TRUE when any intersection exists; pickedGeometry/pickedPoint/pickedNormal/pickedTextureCoordinate emitted per sortOrder; PickableGroup.pickable and objectType filter candidacy; matchCriterion (MATCH_ANY/MATCH_EVERY/MATCH_ONLY_ONE) qualifies targets. Spec requirement is unambiguous for all 5 nodes across levels 1–3.
- **Impl** — — All 5 nodes exist as generated data-layer bindings only (generated_cpp_bindings/LinePickSensor.hpp, PointPickSensor.hpp, PrimitivePickSensor.hpp, VolumePickSensor.hpp, PickableGroup.hpp, X3DPickSensorNode.hpp). No System subclass in runtime/ handles these nodes. grep runtime/ for LinePickSensor/PointPickSensor/PrimitivePickSensor/VolumePickSensor/PickableGroup returns zero hits. PickSystem::pickClosest (runtime/scene/PickSystem.hpp:71) is wired exclusively to PointingSensorSystem for pointer-device resolution — not called by any pick-sensor consumer. PickableGroup.pickable field is stored in the binding but never read at runtime. Fields pickingGeometry and pickTarget are stored but never evaluated.
- **Wiki** ◑ — No dedicated wiki subsystem page exists for the Picking component (docs/wiki/subsystems/ has no picking.md or system-picking.md). docs/wiki/coverage.md has no row for the Picking component at all, contrary to the convention that every component/subsystem has a coverage row. The conformance matrix at docs/conformance/components/Picking.md accurately reflects the state (Behaves: ✗ for all sensor nodes, Behaves: — for PickableGroup). system-pointing.md correctly covers PickSystem for pointing-device sensors but makes no claims about Picking-component nodes.
- **Findings** — 1 open / 0 closed. One finding: CONF-PICKSENSOR (status: deferred, severity: minor) — pick-sensor nodes don't run the pick engine against pickingGeometry/pickTarget. Status is accurate; no code has landed to close it. No resolution field is present in findings.yaml. The earlier context snippet showing a 'resolution' field was noise from a neighboring finding (CONF-WORLDBOUNDS/CONF-TOUCHTIME); CONF-PICKSENSOR itself has no resolution.

  **New findings (1):**
  - ⚪ **Picking component has no coverage.md row** — grep -i 'picking|X3DPickSensor|LinePickSensor|PickableGroup' /home/ben/code/x3d-cpp/docs/wiki/coverage.md returns zero hits. Per CLAUDE.md and the coverage table convention, every component/subsystem should have a row. No entry exists for the Picking component or for any of its 5 nodes, making the table incomplete for cross-reference searches.

### PointingDeviceSensor

> All four nodes are fully implemented and wired, two fixed findings are confirmed in code, one open finding (CONF-CRITIC-3) is implemented but lacks a targeted test; the wiki has two stale claims — wrong ctest -R filter names and a deprecated BACKLOG.md citation.

- **Spec** ✓ — Four nodes — TouchSensor (§20.4.4), PlaneSensor (§20.4.2), SphereSensor (§20.4.3), CylinderSensor (§20.4.1) — all covered by spec RAG with unambiguous normative prose. Field tables, constraints, and three-condition touchTime rule (§20.4.4) are clear. Grab exclusivity (§20.2.1), drag common protocol (§20.2.2), nearest-geometry resolution (§20.2.3) are well-specified.
- **Impl** ◑ — All four sensor types are fully wired in runtime/events/PointingSensorSystem.hpp. Pick resolution, grab lifecycle, isOver/isActive edge detection, all drag-math functions (PlaneDrag.hpp, SphereDrag.hpp, CylinderDrag.hpp), autoOffset, hitPoint_changed/hitNormal_changed/hitTexCoord_changed emission — all implemented. DS-1 (CylinderSensor disk mode uses Y=0 plane, confirmed CylinderDrag.hpp:87-90) and DS-2 (disabled-mid-drag deactivation, confirmed PointingSensorSystem.hpp:84-95) are verifiably fixed in code. CONF-CRITIC-3: the three-condition touchTime check is implemented (PointingSensorSystem.hpp:101-102, 131: `stillOver = pick.hit && resolve(pick) == active_; if (pressWasOver_ && stillOver)`) but the specific scenario where the release pick resolves over a DIFFERENT sensor (not the grabbed one) has no dedicated test, leaving the finding open.
- **Wiki** ◑ — docs/wiki/subsystems/system-pointing.md is otherwise accurate — all cited file paths exist, class names are correct, spec section refs (§20.2.1–§20.4.4) are accurate, drag math descriptions match code. Two stale claims found: (1) system-pointing.md:149,152 gives ctest filter commands `ctest --preset dev -R x3d_pointing_sensor` and `ctest --preset dev -R x3d_drag_math`, but the registered ctest NAME is `x3d_events_tests` (CMakeLists.txt:1986 `add_test(NAME x3d_events_tests ...)`); these -R patterns match nothing. (2) system-pointing.md:179 cites `docs/superpowers/BACKLOG.md` M2D-1 / M2D-3 entries, but BACKLOG.md is deprecated (54 lines, no M2D entries); those entries exist only in git history.
- **Findings** — 1 open / 2 closed. Open: CONF-CRITIC-3 (TouchSensor §20.4.4 — touchTime condition-3 still-over-at-release pick re-resolution not covered by a dedicated test). Closed: DS-1 (CylinderSensor disk mode Y=0 plane, commit 5eef411, confirmed in CylinderDrag.hpp:87-90), DS-2 (disabled-mid-drag deactivation, commit 8652034, confirmed in PointingSensorSystem.hpp:84-95).

  **New findings (2):**
  - 🟡 **Wiki ctest -R filter names do not match registered test name** — docs/wiki/subsystems/system-pointing.md:149 claims `ctest --preset dev -R x3d_pointing_sensor`; system-pointing.md:152 claims `ctest --preset dev -R x3d_drag_math`. CMakeLists.txt:1986 registers a single test as `add_test(NAME x3d_events_tests ...)`. The -R patterns x3d_pointing_sensor and x3d_drag_math match nothing; the correct command is `ctest --preset dev -R x3d_events_tests`.
  - ⚪ **Wiki cites deprecated BACKLOG.md for M2D-1/M2D-3 entries that no longer exist there** — docs/wiki/subsystems/system-pointing.md:179 says `see docs/superpowers/BACKLOG.md M2D-1 / M2D-3 entries`. BACKLOG.md is 54 lines, deprecated since 2026-06-22, contains no M2D entries. The entries exist only in git history (git log -p docs/superpowers/BACKLOG.md). CLAUDE.md explicitly marks BACKLOG.md as deprecated.

### Rendering

> The Rendering component extraction is largely functional, but TriangleFanSet and TriangleStripSet silently produce flat normals instead of the spec-required per-vertex averages (MSH-2 averagedSets gap at MeshBuilder.hpp:1778-1781), and ClipPlane has no extraction logic whatsoever beyond the shader vocabulary reservation.

- **Spec** ✓ — All 15 nodes fully specified in ISO/IEC 19775-1 §11 (rendering.md). Key normative rules confirmed: colorPerVertex is ignored/always TRUE for IndexedTriangleFanSet (§11.4.7), IndexedTriangleStripSet (§11.4.9), TriangleFanSet (§11.4.13), TriangleStripSet (§11.4.15); normalPerVertex=TRUE requires per-vertex averaging for those same four nodes. ClipPlane (L5) carries enabled/plane fields (§11.4.1). CoordinateDouble uses MFVec3d (§11.4.5).
- **Impl** ◑ — Most Rendering geometry nodes are wired in MeshBuilder.hpp. IndexedLineSet/LineSet/PointSet produce correct Lines/Points topology (B4). EXT-002 fix (faceNo per-fan/strip for colorPerVertex=FALSE) is present at MeshBuilder.hpp:1617, 1635, 1663, 1677. Two gaps: (1) TriangleFanSet and TriangleStripSet are absent from the averagedSets array (MeshBuilder.hpp:1778-1781) so normalPerVertex=TRUE vertex-averaging is never applied to them, only to their Indexed variants — violating §11.4.13/§11.4.15. (2) ClipPlane exists only in generated bindings and ShaderUniformVocabulary.hpp:177-180; SceneExtractor.hpp has no DFS code to collect ClipPlane nodes and populate numClipPlanes/clipPlane[] uniforms — ClipPlane geometry effect is fully inert. CoordinateDouble silently produces empty meshes because getField<vector<SFVec3f>> at MeshBuilder.hpp:1462 throws on MFVec3d and returns empty.
- **Wiki** ◑ — No dedicated Rendering component wiki page exists and none is listed in docs/wiki/coverage.md. Rendering geometry nodes are covered incidentally by docs/wiki/subsystems/extract.md (lines 122, 127), which correctly lists all handled node types. The extract.md does not document the TFS/TSS normal-averaging gap or the colorPerVertex-ignored-for-fan/strip spec requirement. The shaders.md mentions numClipPlanes/clipPlane[] in the vocabulary table (line 72) but does not note that ClipPlane extraction is unimplemented. No page maps directly to the Rendering component as a first-class entry.
- **Findings** — 0 open / 1 closed. One finding in docs/conformance/findings.yaml tagged component: Rendering — EXT-002 (major, CLOSED at commit fe4d730). EXT-002 fix is verified present in code: MeshBuilder.hpp:1617 (IndexedTriangleFanSet flush), :1635 (IndexedTriangleStripSet flush), :1663 (TriangleFanSet loop), :1677 (TriangleStripSet loop) all increment faceNo per-primitive not per-triangle. Commit fe4d730 is not in the local branch history but the fix is in the working tree, consistent with a squash-merge to main.

  **New findings (3):**
  - 🟡 **TriangleFanSet and TriangleStripSet excluded from MSH-2 vertex-normal averaging** — MeshBuilder.hpp:1778-1781 defines averagedSets as {IndexedTriangleSet, TriangleSet, IndexedTriangleStripSet, IndexedTriangleFanSet, QuadSet, IndexedQuadSet}. TriangleFanSet and TriangleStripSet are absent. The spec §11.4.13 (TriangleFanSet) and §11.4.15 (TriangleStripSet) both require: 'if normalPerVertex is TRUE, the normal shall be the average of all triangles within that [fan/strip] sharing that vertex.' Without inclusion in averagedSets, these two nodes receive flat per-triangle normals even when normalPerVertex=TRUE and no Normal node is authored. No test in mesh_builder_b7_test.cpp covers TFS or TSS for this averaging requirement.
  - ⚪ **colorPerVertex not ignored for fan/strip sets — EXT-002 fix incomplete per spec** — Spec §11.4.7 (IndexedTriangleFanSet), §11.4.9 (IndexedTriangleStripSet), §11.4.13 (TriangleFanSet), §11.4.15 (TriangleStripSet) state: 'The value of the colorPerVertex field is ignored and always treated as TRUE.' MeshBuilder.hpp:1527 passes attrs.colorPerVertex to AttrResolvers::pickIndex inside the addTriangle lambda. If an author sets colorPerVertex=FALSE on these nodes, the code applies one color per fan/strip (via faceNo) rather than per-vertex, violating the spec's ignore requirement. EXT-002 corrected faceNo granularity but did not enforce the ignore-colorPerVertex constraint.
  - ⚪ **No dedicated wiki page for the Rendering component** — docs/wiki/coverage.md lists no entry for a Rendering subsystem page. ls docs/wiki/subsystems/ shows no rendering.md or geometry.md. The Rendering component's 15 nodes (ClipPlane, Color, ColorRGBA, Coordinate, CoordinateDouble, IndexedLineSet, and the triangle/fan/strip families) are covered only incidentally in extract.md. The MSH-2 averaging gap for TFS/TSS and the colorPerVertex-ignored requirement are undocumented anywhere in the wiki.

### RigidBodyPhysics

> RigidBodyPhysics has substantive partial implementation (inertia/COM, friction/bounce, 3 joint types all wired), but docs/wiki/subsystems/physics.md has four concrete drift issues: stale ShapeDesc/addBody interface snippet, inertia/centerOfMass incorrectly marked ignored, collider field incorrectly listed as ignored, and a stale reference to the deprecated BACKLOG.md tracker.

- **Spec** ✓ — 14 nodes fully spec-legible via spec_rag. §37.4.10 RigidBody (34 fields) and §37.4.11 RigidBodyCollection (27 fields) normative text is unambiguous. Level 1 nodes (CollidableOffset, CollidableShape, CollisionCollection, CollisionSensor, CollisionSpace) and Level 2 nodes (BallJoint, Contact, DoubleAxisHingeJoint, MotorJoint, RigidBody, RigidBodyCollection, SingleAxisHingeJoint, SliderJoint, UniversalJoint) all have clear ISO prose anchors.
- **Impl** ◑ — Substantive partial implementation. Wired and verified: Box/Sphere/Cylinder shapes (PhysicsBackend.hpp ShapeDesc::Kind::{Box,Sphere,Cylinder}), gravity, RigidBody.enabled (PhysicsSystem.hpp:112), forces/torques (PhysicsSystem.hpp:332 applyBodyForces), autoDamp + damping factors, useGlobalGravity (PhysicsSystem.hpp:141-143 setGravityFactor), inertia/centerOfMass (PhysicsSystem.hpp:130-135 MassProperties), BallJoint/SingleAxisHingeJoint/SliderJoint constraints (PhysicsBackend.hpp ConstraintDesc::Kind), hinge limits, CollisionSensor contacts/intersections/isActive (ContactReporter.hpp), CollisionCollection friction/bounce via collider (PhysicsSystem.hpp:165). Not wired: DoubleAxisHingeJoint/MotorJoint/UniversalJoint (CONF-RBP-JOINTS/deferred), joint output events (CONF-RBP-JOINT-OUT/deferred), mesh/IndexedFaceSet shapes + CollidableOffset + compound (CONF-RBP-GEOM/deferred), RigidBodyCollection solver-tuning fields iterations/errorCorrection/etc (CONF-RBP-SOLVER/deferred), per-body sleep thresholds (CONF-RBP-DAMP/deferred), massDensityModel + finiteRotationAxis (CONF-RBP-INERTIA/deferred).
- **Wiki** ◑ — docs/wiki/subsystems/physics.md is materially partially stale with 4 distinct drifts. (1) Line 23 Key files table + lines 35-44 interface snippet still shows ShapeDesc (Box | Sphere) and the old addBody(WorldHandle, const ShapeDesc&, float mass, bool fixed, ...) signature — actual code has ShapeDesc::Kind::Cylinder added and addBody now takes const MassProperties& (runtime/physics/PhysicsBackend.hpp:211-214). (2) Line 76 RigidBody field table marks inertia and centerOfMass as ignored / shape-derived, but PhysicsSystem.hpp:130-135 reads both and passes them via MassProperties.overrideInertia to the backend — inertia/COM shipped in 3a9595c per CONF-RBP-INERTIA. Only massDensityModel + finiteRotationAxis remain unread. (3) Line 79 RigidBodyCollection section lists collider as Ignored — but PhysicsSystem.hpp:165 reads collection->getCollider() to pull CollisionCollection.frictionCoefficients/bounce (CONF-RBP-FRICTION-BOUNCE closed/e038494). (4) Line 66 refers to docs/superpowers/BACKLOG.md as the deferral backlog, but BACKLOG.md was deprecated 2026-06-22; deferrals now live in findings.yaml / GitHub Project. Line 101 scope paragraph also says deferred follow-ons include sleep/inertia/COM fields — inertia/COM have shipped so that phrase misleads.
- **Findings** — 0 open / 8 closed. 13 total findings for RigidBodyPhysics in findings.yaml. Deferred=7 (CONF-RBP-GEOM, CONF-RBP-JOINTS, CONF-RBP-JOINT-OUT, CONF-RBP-DAMP, CONF-RBP-INERTIA, CONF-RBP-SOLVER, CONF-RBP-CAP). Fixed=5 (CONF-RBP collision-sensor, CONF-RBP-FORCES, CONF-RBP-HINGE-LIMITS, CONF-RBP-DETERMINISM, CONF-RBP-ENABLED). Closed=1 (CONF-RBP-FRICTION-BOUNCE). The schema uses open/deferred/fixed/closed; reporting open=0, closed=8 (fixed+closed combined as resolved/done). All 7 deferred findings still apply — code does not resolve any of them. All 5 fixed and 1 closed finding are verified in the code.

  **New findings (4):**
  - 🟡 **physics.md Key files table and interface snippet show stale ShapeDesc and addBody signature** — docs/wiki/subsystems/physics.md:23 shows 'ShapeDesc (Box | Sphere)'; lines 35-44 code snippet shows 'enum class Kind { Box, Sphere }' and 'addBody(WorldHandle, const ShapeDesc&, float mass, bool fixed, ...)'. Actual code: runtime/physics/PhysicsBackend.hpp:63 has 'enum class Kind { Box, Sphere, Cylinder }' and line 211-214 shows 'addBody(WorldHandle world, const ShapeDesc &shape, const MassProperties &mass, bool fixed, ...)' with MassProperties replacing bare float.
  - 🟡 **physics.md field table incorrectly marks inertia and centerOfMass as ignored** — docs/wiki/subsystems/physics.md:76 row: '| inertia, centerOfMass, massDensityModel, finiteRotationAxis | ❌ ignored | inertia/COM always shape-derived |'. Code at runtime/physics/PhysicsSystem.hpp:130-135 reads body->getInertia(), sets mp.overrideInertia=true when non-identity, and sets mp.centerOfMass. CONF-RBP-INERTIA (deferred) confirms inertia/COM shipped in commit 3a9595c. Only massDensityModel and finiteRotationAxis remain unread.
  - 🟡 **physics.md RigidBodyCollection field list incorrectly marks collider as Ignored** — docs/wiki/subsystems/physics.md:79 states 'Ignored: collider, iterations, ...' but runtime/physics/PhysicsSystem.hpp:165 reads 'collection->getCollider()' to obtain CollisionCollection.frictionCoefficients and bounce. CONF-RBP-FRICTION-BOUNCE (closed/e038494) documents this. collider IS read for friction/bounce; only the solver-tuning fields are truly ignored.
  - ⚪ **physics.md references deprecated BACKLOG.md as the deferral tracker** — docs/wiki/subsystems/physics.md:66 says 'deferral backlog (docs/superpowers/BACKLOG.md) point here'. Per CLAUDE.md, BACKLOG.md was deprecated 2026-06-22; deferrals now live in docs/conformance/findings.yaml (behavioral/spec gaps) or the GitHub Project (engineering deferrals). The file still exists but is deprecated. Also, physics.md:101 scope paragraph lists 'sleep/inertia/COM fields' as deferred follow-ons but inertia/COM have shipped.

### Scripting

> Script node lifecycle, marshalling, and seam are fully wired and tested; the only open gaps are the two deferred URL-loading findings (CONF-CRITIC-2, SCR-005) that require the AssetResolver seam to implement external-URL fetch and autoRefresh reload.

- **Spec** ✓ — ISO/IEC 19775-1 §29 is fully covered by docs/conformance/components/Scripting.md. One node (Script, L1). Normative requirements span §29.2.2 (field marshalling per ISO/IEC 19777-1 ECMAScript binding), §29.2.3 (initialize/shutdown lifecycle), §29.2.4 (eventsProcessed), §29.2.5 (prepareEvents once-per-timestamp before ROUTE processing), §29.3.1 (URL loading, autoRefresh, autoRefreshTimeLimit), and §29.4.1 (directOutput/mustEvaluate gates, SAI surface). Spec requirements are unambiguous and tracked.
- **Impl** ◑ — All §29.2 lifecycle phases are wired in runtime/script/ScriptSystem.hpp. The once-per-timestamp prepareEvents guard is at line 330 (havePrepared_/preparedAt_ members). Shutdown on world unload is at line 82–87 (~ScriptSystem destructor). eventsProcessed readback wired at line 157–173 (runEventsProcessed). SaiContext.hpp enforces directOutput gate (throws std::logic_error, line 97–98) and mustEvaluate eager/deferred paths (ScriptSystem.hpp line 161–169). EcmaScriptBackend.cpp has complete SF/MF marshalling including SFMatrix3f/4f/3d/4d (lines 379–393) and SFImage (lines 207–227 pushSFImage/toSFImage). QuickJS second backend exists (runtime/script/QuickJsBackend.{hpp,cpp}) with CI-gated swap-test. INERT: autoRefresh/autoRefreshTimeLimit fields exist in generated bindings (generated_cpp_bindings/X3DScriptNode.cpp) but no reload timer or external URL fetch logic is present in runtime/script/ScriptSystem.hpp — confirmed by grep; CONF-CRITIC-2 and SCR-005 remain open.
- **Wiki** ✓ — docs/wiki/subsystems/system-script-sai.md accurately describes the implementation. All named file paths exist: ScriptSystem.hpp, ScriptEngine.hpp, SaiContext.hpp, EcmaScriptBackend.{hpp,cpp}, QuickJsBackend.{hpp,cpp}, and all test files under runtime/script/tests/. Interface signatures in the wiki match code exactly (ScriptEngine virtual methods, SaiContext get/setField/addRoute/deleteRoute/getName/getVersion/currentTime/currentFrameRate/print). One minor under-specification: the wiki lists 'ctest --preset dev -R x3d_script_cdata_audit' (line 164) without citing its source file path — the test is actually at runtime/codecs/tests/script_cdata_audit_test.cpp, not under runtime/script/tests/ where all other listed tests live. This is incomplete attribution rather than a factual error, as the ctest name itself is correct (confirmed in CMakeLists.txt line 1866).
- **Findings** — 2 open / 6 closed. 8 findings total tagged nodes: [Script]. Open/deferred: SCR-005 (autoRefresh/autoRefreshTimeLimit URL reload + re-initialize absent, §29.3.1) and CONF-CRITIC-2 (Script external-URL loading + auto-refresh interaction unimplemented, §29.3.1) — both deferred, unblocked by ADR-0023 AssetResolver seam. Fixed (6): SCR-001 (prepareEvents once-per-timestamp, code verified at ScriptSystem.hpp:330), SCR-002 (shutdown on unload, verified at ScriptSystem.hpp:82–87), SCR-003 (eventsProcessed JS readback, verified at EcmaScriptBackend.cpp:891), SCR-004 (prepareEvents JS readback, verified at EcmaScriptBackend.cpp:776), SCR-006 (SFMatrix3f/4f/3d/4d marshalling, verified at EcmaScriptBackend.cpp:379–393), SCR-007 (SFImage+MFImage+MFMatrix marshalling, verified at EcmaScriptBackend.cpp:207–227). Note: commit hashes cited in findings.yaml (f2fd324, ff23503, a78da16, f5001cc) do not appear in current log (likely squash-merged), but the code changes they represent are confirmed present.

  **New findings (1):**
  - ⚪ **x3d_script_cdata_audit test source file not cited in wiki, and located outside the script subsystem directory** — docs/wiki/subsystems/system-script-sai.md line 164 lists 'ctest --preset dev -R x3d_script_cdata_audit' without a source file path, unlike every other test in the section. The actual file is runtime/codecs/tests/script_cdata_audit_test.cpp (CMakeLists.txt line 1866), not under runtime/script/tests/ where all other Script subsystem tests reside. Minor: the ctest name is correct, but the wiki omits the file path and the cross-subsystem placement is undocumented.

### Shaders

> ComposedShader extraction is intentionally deferred (RenderItem::shaderProgram is never set by any extractor codepath), and vertex attribute nodes (FloatVertexAttribute/Matrix3/4VertexAttribute) are silently dropped by buildAttrs() — neither gap is tracked in findings.yaml.

- **Spec** ✓ — 8 nodes at L1 (ComposedShader, FloatVertexAttribute, Matrix3VertexAttribute, Matrix4VertexAttribute, PackagedShader, ProgramShader, ShaderPart, ShaderProgram). Spec requires ComposedShader to compose source from ShaderPart URL-loaded GLSL, expose dynamic <field> declarations as typed uniforms, and emit isSelected/isValid output events. FloatVertexAttribute/Matrix*VertexAttribute supply per-vertex custom data via the 'attrib' child slot. Spec is unambiguous for this component.
- **Impl** ◑ — Generated bindings exist for all 8 nodes and parsing is wired (reader_audit_test.cpp:337). Descriptor infrastructure is complete and tested: ShaderStageDesc/ShaderFieldBinding/ShaderProgramDesc (runtime/extract/RenderItem.hpp:440-461), ShaderUniformVocabulary (runtime/extract/ShaderUniformVocabulary.hpp), buildBindingPlan() (runtime/extract/ShaderBindingPlan.hpp:78). BUT: (1) RenderItem::shaderProgram (SceneExtractor.hpp:100) is never assigned in any extractor codepath — SceneExtractor::emit() at line 625-696 sets material/mesh/castShadow but not shaderProgram, making PATH 4 in poc_renderer (main.cpp:1443-1444) permanently unreachable. (2) buildAttrs() at MeshBuilder.hpp:514-549 reads 'normal', 'color', 'texCoord' children but ignores 'attrib' children entirely — FloatVertexAttribute/Matrix3VertexAttribute/Matrix4VertexAttribute are never surfaced. (3) PackagedShader/ProgramShader/ShaderProgram have no extraction logic at all. cpu_raster --frag applies an author shader globally, not via RenderItem::shaderProgram.
- **Wiki** ◑ — docs/wiki/subsystems/shaders.md correctly describes the deferred status of ComposedShader extraction, the descriptor infrastructure, buildBindingPlan(), and the cpu_raster interpreter path. One concrete drift: the ShaderProgramDesc code snippet at shaders.md line 52 shows 'std::string language;' as a struct member, but the actual struct at runtime/extract/RenderItem.hpp:454-461 has no language field (fields are: stages, fields, isSelected, isValid, lastError, attributeBindings). All cited file paths and class names otherwise exist. FloatVertexAttribute/Matrix*VertexAttribute extraction gap is not mentioned anywhere in the wiki.
- **Findings** — 0 open / 0 closed. No findings tagged component: Shaders exist in docs/conformance/findings.yaml (confirmed by full grep and manual scan of all 137 entries). The ComposedShader extraction gap is documented in the wiki as a known deferred follow-on, but is not tracked as a conformance finding. The vertex attribute node gap is tracked nowhere.

  **New findings (3):**
  - ⚪ **ShaderProgramDesc code snippet in wiki includes non-existent 'language' field** — docs/wiki/subsystems/shaders.md line 52 shows 'std::string language;' in the ShaderProgramDesc struct, but runtime/extract/RenderItem.hpp:454-461 defines ShaderProgramDesc with fields: stages, fields, isSelected, isValid, lastError, attributeBindings — no language field.
  - 🟡 **FloatVertexAttribute/Matrix3VertexAttribute/Matrix4VertexAttribute silently dropped during extraction — not tracked in findings.yaml** — runtime/extract/MeshBuilder.hpp:514-549 (buildAttrs()) reads 'normal', 'color', 'texCoord' child nodes but never reads 'attrib' children. All three vertex attribute node types (containerField: attrib) are therefore unreachable from any extraction path. Not mentioned in shaders.md and no conformance finding exists.
  - 🟡 **ComposedShader extraction gap (RenderItem::shaderProgram never set) not tracked as a conformance finding** — SceneExtractor::emit() at runtime/extract/SceneExtractor.hpp:625-696 never assigns RenderItem::shaderProgram. poc_renderer/main.cpp:1443-1445 comments 'Currently unreachable — ComposedShader extraction not yet implemented'. The wiki documents this as a deferred follow-on, but docs/conformance/findings.yaml has zero Shaders entries, so the gap is invisible to the conformance gate.

### Shape

> All 9 Shape findings are closed and verified in code; the component is fully implemented, but two stale comments — one in extract-textures.md (MAT-006 labelled deferred) and two in MaterialSystem.hpp (null-material behaviour described as Phong grey) — should be corrected.

- **Spec** ✓ — §12.4.2 (Appearance), §12.4.5 (Material), §12.4.8 (Shape), §12.4.10 (UnlitMaterial), §17.2.2.4 (Unlit model) all unambiguous. Null material under present Appearance => Unlit white (1,1,1) per §12.4.2 and §17.2.2.4 rule 2. All 10 Shape-component nodes (AcousticProperties, Appearance, FillProperties, LineProperties, Material, PhysicalMaterial, PointProperties, Shape, TwoSidedMaterial, UnlitMaterial) have normative declarations in the spec RAG. No ambiguity in the spec text for the tracked clauses.
- **Impl** ✓ — All 9 MAT findings confirmed closed in code: MAT-001 (null material => Unlit white at MaterialSystem.hpp:194-200), MAT-002 (Shininess slot at line 337-338), MAT-003 (Ambient slot at line 339-340), MAT-004 (occlusionStrength read at line 237-238), MAT-005 (normalScale at line 242), MAT-006 (backMaterial dispatch at lines 247-284), MAT-007 (texCoordMapping via mappingOf at line 137), MAT-008 (ORM comment in RenderItem.hpp:218-235), MAT-009 (GL_SRGB8_ALPHA8 + linearToSRGB in poc_renderer). Shape fields visible (SceneExtractor.hpp:498-500), castShadow (line 632), bboxCenter/bboxSize (BoundsSystem.hpp:82-85) are all wired. bboxDisplay is not extracted — it is a renderer-side hint and is not tracked as a gap.
- **Wiki** ◑ — Two stale claims found. (1) docs/wiki/subsystems/extract-textures.md line 129 reads 'Conformance finding MAT-006 (deferred — Appearance.backMaterial two-sided extraction)' but MAT-006 has been closed (commit 4c47b58); the backMaterial path is fully implemented in MaterialSystem.hpp:247-284. (2) runtime/extract/MaterialSystem.hpp lines 14-15 and 168 (file-header comment and function-level banner) both state 'null material under a PRESENT Appearance => Phong 0.8 grey (the spec default Material)' — but the actual code at lines 190-200 returns Unlit white (1,1,1) as the MAT-001 fix requires. The in-source comments are stale pre-fix text and contradict both spec and implementation. The materials.md wiki page itself is accurate and consistent with code.
- **Findings** — 0 open / 9 closed. All 9 Shape-component findings (MAT-001 through MAT-009) are closed in findings.yaml. Code verification confirms every fix is present. No open or deferred findings remain for this component.

  **New findings (2):**
  - ⚪ **extract-textures.md incorrectly describes MAT-006 as deferred** — docs/wiki/subsystems/extract-textures.md:129 reads 'Conformance finding MAT-006 (deferred — Appearance.backMaterial two-sided extraction)'. findings.yaml:1076 shows status: closed, commit: 4c47b58. The backMaterial extraction is fully implemented at runtime/extract/MaterialSystem.hpp:247-284.
  - ⚪ **MaterialSystem.hpp has stale header comments claiming null material => Phong grey** — runtime/extract/MaterialSystem.hpp:14-15 states 'null material under a PRESENT Appearance => Phong 0.8 grey (the spec default Material)' and line 168 repeats 'Appearance present, material null => Phong 0.8 grey'. Actual code at lines 190-200 returns MaterialModel::Unlit with emissive=(1,1,1) (the MAT-001 fix). These are pre-fix artifacts that contradict both §12.4.2 and the implementation.

### Sound

> Sound component is partially implemented with a well-maintained AudioBackend seam (6 of 21 nodes wired, stereo spatial path proven via swap-test), but BiquadFilter Lowshelf/Highshelf/Peaking filter types silently pass audio through unfiltered due to a missing dBGain parameter in the seam contract — an untracked correctness gap in an already-built node.

- **Spec** ✓ — 21 nodes across L1 (Sound, AudioClip) and L2 (OscillatorSource, BiquadFilter, Gain, AudioDestination, SpatialSound, ListenerPointSource, channel nodes, processing nodes, etc.). Normative rules are unambiguous: §16.4.17 ellipsoid attenuation, §16.4.2 AudioClip time-lifecycle, §16.4.15 OscillatorSource frequency/detune/periodicWave, §16.4.4 BiquadFilter types including Lowshelf/Highshelf/Peaking, §8.2.4.1 X3DTimeDependentNode lifecycle. Spec RAG returned full node definitions with field ranges and prose. No ambiguity identified.
- **Impl** ◑ — SoundSystem.hpp:192-236 (buildChild) wires: OscillatorSource, BiquadFilter, Gain, SpatialSound — all others return kInvalidNodeHandle at line 225. attach() (line 89) accepts AudioDestination and SpatialSound only. pushParams() (lines 247-261) reads frequency/detune/gain/Q — no enabled check anywhere. update() (line 158) loops map_ → pushParams() only, no time-lifecycle logic. Confirmed inert: (1) enabled field — absent from buildChild and pushParams (SND-1); (2) startTime/stopTime/pauseTime/resumeTime/loop/isActive/isPaused/elapsedTime — absent from SoundSystem entirely (SND-2); (3) AudioDestination.gain — not read in attach() despite maxChannelCount being read at line 95 (SND-7); (4) Lowshelf/Highshelf/Peaking BiquadFilter — BuiltinDspBackend.cpp:137-140 silently falls through to pass-through (b0=1) when type is one of these three; NodeParams carries no dBGain field (AudioBackend.hpp); (5) 15 of 21 nodes unbuilt (SND-4, SND-5, SND-6, etc.).
- **Wiki** ✓ — docs/wiki/subsystems/sound.md is accurate and current. All 10 key file paths verified to exist. ADRs 0020 and 0026 exist. The file table accurately notes 'Lowshelf/Highshelf/Peaking pass-through pending a dB-gain param' (BuiltinDspBackend.cpp row). Open/deferred gaps are correctly described. The Purpose section lists exactly the 6 wired nodes (OscillatorSource, BiquadFilter, Gain, AudioDestination, SpatialSound, ListenerPointSource) without overclaiming Sound (§16.4.17 ellipsoidal node). attach() behavior described at line 91 matches code exactly. One minor omission: the Lowshelf/Highshelf/Peaking pass-through is noted only in prose, not as a tracked finding.
- **Findings** — 3 open / 0 closed. 9 findings tagged component:Sound in findings.yaml: SND-1 (open), SND-2 (open), SND-3 (deferred), SND-4 (deferred), SND-5 (deferred), SND-6 (deferred), SND-7 (open), SND-8 (deferred), SND-9 (deferred). TDN-5 cross-listed in Sound.md matrix but has no component: field in findings.yaml. All 3 open findings verified still accurate: SND-1 — enabled absent from SoundSystem.hpp:192-261 confirmed; SND-2 — no time-lifecycle logic in update() (line 158) confirmed; SND-7 — AudioDestination.gain not read in attach() (line 89-103) confirmed. No false closures found.

  **New findings (1):**
  - 🟡 **BiquadFilter Lowshelf/Highshelf/Peaking silently pass-through — untracked seam gap** — runtime/sound/dsp/BuiltinDspBackend.cpp:137-140: case FilterType::Lowshelf / case FilterType::Highshelf / case FilterType::Peaking fall through to pass-through (b0=1, all other coefficients 0) because the seam's NodeParams struct (runtime/sound/AudioBackend.hpp) carries no dBGain parameter. A BiquadFilter node with type LOWSHELF, HIGHSHELF, or PEAKING is wired and active but produces unfiltered output silently. This is distinct from SND-5 (fully-unbuilt processing nodes) — BiquadFilter IS built and wired. Not currently tracked in docs/conformance/findings.yaml.

### Text

> Text extraction is substantially wired (layout engine + quad emission + output fields), with 3 open spec-conformance gaps (TXT-2, TXT-4, TXT-5) confirmed in code and no new gaps discovered.

- **Spec** ✓ — ISO/IEC 19775-1:2023 §15 is well-covered by spec_rag: §15.4.2 Text (string, length, maxExtent, textBounds, lineBounds, origin, solid) and §15.4.1 FontStyle (family, style, horizontal, leftToRight, topToBottom, justify, size, spacing) are fully described. §15.2.2.3 direction/justification tables (minor-axis END, MIDDLE, BEGIN, FIRST for horizontal and vertical) are unambiguous. TXT-2 and TXT-4 map directly to identifiable formula errors in §15.2.2.3 Table 15.4/15.5. TXT-5 maps to §15.2.2.2 family fallback requirement. No spec ambiguity for any open finding.
- **Impl** ◑ — Core pipeline is wired: TextLayout.hpp:146 computeTextLayout implements §15.2.2.3 justify logic; TextExtract.hpp:221 buildTextMesh reads FontStyle+Text fields via reflection, calls the layout engine, emits CCW glyph quads (isGlyphMesh=true, solid=false), and writes back textBounds/lineBounds/origin via setTextOutputs (TextExtract.hpp:339). SceneExtractor.hpp:523 T-TEXT dispatch hook wires everything into the pipeline. FontMetrics seam (FontMetrics.hpp) is proven generic (stb_truetype backend, ADR-0025). Three confirmed gaps: (1) TXT-2 — END minor axis, horizontal, topToBottom=FALSE: code at TextLayout.hpp:294 computes firstBaselineMinor = +(N-1)*step - ascender[N-1] but the correct formula requires -(N-1)*step - ascender[N-1]; the sign on the step term is wrong, placing the block too high. (2) TXT-4 — END minor axis, vertical, leftToRight=FALSE: same sign error at TextLayout.hpp:333. (3) TXT-5 — family[] MFString fallback: TextExtract.hpp:149 does familyOut = fam[0], picking only the first listed family with no iteration to find a supported one. Hardcoded 0.8/-0.2 ascender/descender in makeLayoutMetricsAdapter (TextExtract.hpp:189) is a known deferral (T-TEXT-D2, GitHub Project card).
- **Wiki** ◑ — docs/wiki/subsystems/extract-text.md is substantively accurate: all cited file paths exist (FontMetrics.hpp, TextLayout.hpp, TextExtract.hpp, SceneExtractor.hpp, MeshBuilder.hpp, StbttFontMetrics.hpp/.cpp, font_metrics_tests.cpp, text_layout_test.cpp, text_extract_test.cpp); test counts match code (19 layout tests, 6 extract tests); interface signatures match current headers; ADR-0025 (docs/wiki/decisions/0025-fontmetrics-second-backend-swap-test.md) exists. One minor drift: the Related section (line 175) cites docs/superpowers/BACKLOG.md for T-TEXT-D1 and T-TEXT-D2 deferrals, but BACKLOG.md was deprecated 2026-06-22 and those deferrals now live as GitHub Project cards per CLAUDE.md policy. Not materially wrong (the BACKLOG reference is historical and the file still exists as a migration map), but the wiki should link to the GitHub Project instead.
- **Findings** — 3 open / 1 closed. findings.yaml entries: TXT-1 [fixed] — justify MIDDLE minor axis block-centering; fix verified in TextLayout.hpp:263-277 (correct asymmetric formula using ascender/descender of first and last line). TXT-2 [open] — justify END minor axis, horizontal, topToBottom=FALSE mis-places block; confirmed bug at TextLayout.hpp:294. TXT-4 [open] — justify END minor axis, vertical, leftToRight=FALSE mis-places columns; confirmed bug at TextLayout.hpp:333. TXT-5 [open] — family[] fallback not iterated; confirmed at TextExtract.hpp:149. All three open findings still apply. TXT-3/6/7/8/14/15 were adversarially evaluated and rejected (0-1/3 confirmation per findings.yaml line 1026 comment). T-TEXT-D1 (bidi) and T-TEXT-D2 (exact bounds) are engineering deferrals tracked in GitHub Project, not findings.yaml — correct per project policy.

### TextureProjection

> TextureProjection nodes are data-model stubs only — both TextureProjector and TextureProjectorParallel are absent from LightSystem dispatch and have no runtime extraction or behavioral wiring whatsoever.

- **Spec** ✓ — ISO X3D 4.1 §42 is unambiguous: TextureProjection Level 2 requires TextureProjector (perspective projection) and TextureProjectorParallel (parallel projection), both inheriting X3DTextureProjectorNode < X3DLightNode. Key normative fields: texture (SFNode, X3DTexture2DNode), location, direction, fieldOfView, nearDistance, farDistance, global, on, aspectRatio (outputOnly), shadows, shadowIntensity (TextureProjector) / shadowsIntensity (TextureProjectorParallel). One spec inconsistency noted: the §42.4.2 node grammar spells the shadow intensity field 'shadowsIntensity' (with trailing s) in TextureProjectorParallel, while §42.4.1 uses 'shadowIntensity' in TextureProjector.
- **Impl** — — Both nodes exist as generated C++ binding stubs (generated_cpp_bindings/TextureProjector.hpp, TextureProjectorParallel.hpp, X3DTextureProjectorNode.hpp) with full field reflection tables and range validators. They are registered in X3DNodeFactory.cpp:500-501 and X3DInterfaceRegistry.cpp:244-245. However, runtime/extract/LightSystem.hpp:70-77 lightType() dispatches only DirectionalLight/PointLight/SpotLight — TextureProjector and TextureProjectorParallel are not handled, so they are silently skipped during scene extraction. No TextureProjector references exist anywhere under runtime/ (verified by grep). The nodes parse and store field values but produce no LightDesc, no projection matrix, no texture binding — purely inert data containers.
- **Wiki** — — No subsystem wiki page exists for TextureProjection (ls docs/wiki/subsystems/ confirms no page). docs/wiki/coverage.md has no row for TextureProjection. The conformance matrix at docs/conformance/components/TextureProjection.md correctly marks Extract and Behaves as '—', which is consistent with the na implementation status. No wiki drift to report — the absence of a page is honest given the na state.
- **Findings** — 0 open / 0 closed. docs/conformance/findings.yaml has zero entries tagged component: TextureProjection. The gap (no runtime extraction) is implicitly visible in the conformance matrix '—' columns but is not explicitly tracked as a finding.

  **New findings (2):**
  - 🟡 **TextureProjectorParallel field name mismatch: 'shadowsIntensity' in spec vs 'shadowIntensity' in code** — ISO X3D 4.1 §42.4.2 node grammar: 'SFFloat [in,out] shadowsIntensity 1 [0,1]' (trailing 's'). generated_cpp_bindings/TextureProjectorParallel.cpp:260 exposes the inherited X3DLightNode field as 'shadowIntensity' (no trailing 's') via the reflected FieldTable. An X3D file using 'shadowsIntensity' for TextureProjectorParallel would fail to round-trip the field under the current binding.
  - ⚪ **TextureProjection not dispatched in LightSystem — no findings entry tracks this gap** — runtime/extract/LightSystem.hpp:70-77 lightType() only returns Directional/Point/Spot and sets isLight=false for all other node types including TextureProjector and TextureProjectorParallel. The na implementation status is not recorded in docs/conformance/findings.yaml (0 entries for TextureProjection), leaving the gap untracked for prioritization.

### Texturing

> Texturing extraction is substantially wired and the wiki is accurate; the four tracked deferred findings (TXF-1/2/3, TDN-5) all still apply, and one untracked gap exists: MultiTextureCoordinate silently falls back to default UVs (MeshBuilder.hpp:537-538) without a formal findings.yaml entry.

- **Spec** ✓ — All 11 nodes (Levels 1-3) are defined unambiguously in ISO/IEC 19775-1 §18. Key normative rules verified via spec_rag.py: ImageTexture §18.4.1 (URL, JPEG/PNG required); TextureProperties §18.4.9 (boundaryMode/filter/mipmap table); TextureTransform §18.4.10 (Tc' = -C * S * R * C * T * Tc, with known self-contradiction in spec prose vs. matrix); TextureCoordinateGenerator §18.4.8 (SPHERE/CAMERASPACE* view-dependent at render time); MovieTexture §18.4.2 (time-dependent, duration_changed, startTime/loop semantics). Spec is clear; the only ambiguity (§18.4.10 scale vs. rotate order) is documented in TXF-1.
- **Impl** ◑ — Core extraction path is well-wired: ImageTexture/PixelTexture (MaterialSystem.hpp:100-123 refOf()), MultiTexture channel expansion (MaterialSystem.hpp:138-153 appendSlot()), TextureProperties full §18.4.9 sampler (TextureExtract.hpp:186 extendedSamplerOf()), TextureTransform UV bake (TextureTransform2D.hpp:58 applyTextureTransform() + TextureExtract.hpp:164 applyTextureTransformToMesh()), TextureCoordinateGenerator descriptor surface (TextureExtract.hpp:227 texCoordGenOf()), MFString fallback order (TextureExtract.hpp:252 resolveTextureRefs()), decode seam with two backends stb_image+wuffs (runtime/io/stb/, runtime/io/wuffs/). Known inert paths tracked as open findings: MovieTexture time-lifecycle (TDN-5), TextureCoordinateGenerator actual UV generation (TXF-2, render-time seam by design), MultiTextureTransform/MatrixTextureTransform (TXF-3), TextureTransform order ambiguity (TXF-1). MultiTextureCoordinate: MeshBuilder.hpp:537-538 reads .point on whatever node is in texCoord; if node is MultiTextureCoordinate (which has no .point field), getField returns empty and extraction silently falls back to default UVs — not tracked as a formal finding.
- **Wiki** ✓ — Two wiki pages cover the component: docs/wiki/subsystems/extract-textures.md and docs/wiki/subsystems/system-texture-decode.md. All cited file paths exist and are current (verified ls). Key claims checked: materialOf() null-appearance/material rules match MaterialSystem.hpp:174-199; enrichTextureRefs() with empty texNodes correctly described (sampler set upstream in refOf(), only TexCoordGen attached by enrich); applyTextureTransformToMesh identity early-exit matches TextureTransform2D.hpp:88-95; TextureResolver seam genericity proof (stb_image + wuffs + swap-test) matches runtime/io/{stb,wuffs}/. TXF-1 deferred finding is referenced at extract-textures.md:128. The Extract column '—' in docs/conformance/components/Texturing.md is correct: conformance_view.py sets Extract='n/a' for non-X3DGeometryNode types (script lines 258-259), displayed as '—' — not a staleness bug. Minor: wiki does not mention MultiTextureCoordinate extraction gap.
- **Findings** — 4 open / 0 closed. All 4 Texturing findings are deferred (open): TXF-1 (minor, §18.4.10 scale/rotate order ambiguity — scale-then-rotate in code matches one spec reading); TXF-2 (major, TextureCoordinateGenerator UVs not produced — by-design render-time seam, descriptor surfaced); TXF-3 (minor, MultiTextureTransform + MatrixTextureTransform not handled); TDN-5 (major, MovieTexture time-lifecycle inert — blocked on media seam). Verified all 4 still apply: code search confirms no lifecycle system for MovieTexture, no MultiTextureTransform handling in TextureExtract.hpp:130-155, and TXF-1 scale-then-rotate order still in TextureTransform2D.hpp:70-78.

  **New findings (1):**
  - ⚪ **MultiTextureCoordinate silently falls back to default UVs — not tracked as a finding** — runtime/extract/MeshBuilder.hpp:537-538: getNode(geom, 'texCoord') is followed by getField<MFVec2f>(*tc, 'point', {}). When the texCoord field holds a MultiTextureCoordinate node (§18.4.4), which has no 'point' field (only MFNode 'texCoord'), the getField returns empty and the geometry falls through to generateDefaultTexCoords(). Multi-UV extraction is silently lost. TXF-3 (findings.yaml:1148) covers MultiTextureTransform only; MultiTextureCoordinate has no formal finding, though TXF-3's note references 'T-TEX-D1' as the governing project card.

### Texturing3D

> All 7 Texturing3D nodes parse and reflect correctly via generated bindings, but the extraction layer treats every 3D-specific behavior (TextureTransform3D, TextureCoordinate3D/4D, repeatR) as identity/ignored with no findings tracked and no wiki page documenting the scoping.

- **Spec** ✓ — All 7 nodes fully specified: §33.4.1 ComposedTexture3D, §33.4.2 ImageTexture3D (L2), §33.4.3 PixelTexture3D, §33.4.4 TextureCoordinate3D, §33.4.5 TextureCoordinate4D, §33.4.6 TextureTransform3D, §33.4.7 TextureTransformMatrix3D. Normative rules are unambiguous — repeatR wrap axis, 3D UV coords (MFVec3f/MFVec4f), TRS-in-3D matrix transform, and 2^n depth-slice array contract for ComposedTexture3D all clearly defined.
- **Impl** ◑ — All 7 nodes exist in generated_cpp_bindings/ with correct field tables and reflection thunks (parse/round-trip works). Runtime extraction in TextureExtract.hpp explicitly narrows to 2D-only: (1) textureTransformParamsOf() at line 128-139 silently returns identity for TextureTransform3D and TextureTransformMatrix3D — only 'TextureTransform' and 'TextureTransform2D' are dispatched. (2) extendedSamplerOf() at lines 186-218 reads repeatS and repeatT but never repeatR; ExtendedSamplerParams in TextureTransform2D.hpp has no boundaryModeR field — the R-axis wrap state for all six Texturing3D texture nodes is silently dropped. (3) TextureCoordinate3D and TextureCoordinate4D are never dispatched by texCoordGenOf() or MeshBuilder — 3D UV point arrays are ignored at extraction time. (4) ComposedTexture3D and ImageTexture3D have no extraction logic whatsoever (no source assembly, no texture-ref population). The code comment at runtime/extract/TextureExtract.hpp:128 acknowledges TextureTransform3D is 'out of v1 scope — surfaced as identity; documented.' but finding TXF-3 only covers MatrixTextureTransform for the 2D component.
- **Wiki** ✗ — No wiki subsystem page exists for Texturing3D. coverage.md has no row for this component (confirmed: grep returns zero hits for 'Texturing3D'). extract-textures.md documents the 2D texture extraction pipeline at length but never mentions: the TextureTransform3D identity fallback, the missing repeatR/boundaryModeR handling, the unimplemented 3D coordinate dispatch, or the ComposedTexture3D/ImageTexture3D extraction gaps. The code comment at TextureExtract.hpp:128 asserts the TextureTransform3D scoping is 'documented' — that claim is false; no wiki page carries it.
- **Findings** — 0 open / 0 closed. findings.yaml has zero entries tagged component: Texturing3D. TXF-1, TXF-2, and TXF-3 are all tagged component: Texturing (the 2D component, §18) and cover 2D TextureTransform order ambiguity, TextureCoordinateGenerator render-time gap, and missing MatrixTextureTransform/MultiTextureTransform. None of these reference the Texturing3D (§33) component or its 3D-specific behavioral gaps.

  **New findings (6):**
  - 🟡 **TextureTransform3D and TextureTransformMatrix3D silently fall back to identity — untracked** — runtime/extract/TextureExtract.hpp:128-139: 'A MultiTextureTransform / MatrixTextureTransform / TextureTransform3D is not a single-(s,t) transform — leave identity (documented v1 narrowing).' Only nodeTypeName() == 'TextureTransform' or 'TextureTransform2D' are handled. No findings.yaml entry exists for this gap under component Texturing3D.
  - 🟡 **repeatR field of all Texturing3D texture nodes is never read — R-axis wrap state silently dropped** — runtime/extract/TextureExtract.hpp:191-200 (extendedSamplerOf): reads repeatS and repeatT only. runtime/extract/TextureTransform2D.hpp:216-220 (ExtendedSamplerParams): has boundaryModeS and boundaryModeT but no boundaryModeR. ComposedTexture3D, ImageTexture3D, and PixelTexture3D all carry a repeatR field (confirmed via generated_cpp_bindings/ and spec §33.4.1-3); it is never surfaced to the consumer.
  - 🟡 **TextureCoordinate3D and TextureCoordinate4D point arrays are never extracted** — texCoordGenOf() in runtime/extract/TextureExtract.hpp:227-238 only dispatches TextureCoordinateGenerator nodes, not TextureCoordinate3D/4D. MeshBuilder picks up TextureCoordinate (2D) but nothing dispatches MFVec3f/MFVec4f point data from 3D coordinate nodes. Effect: any geometry using 3D texture coordinates falls through to the default §13 UV projection silently.
  - 🔴 **ComposedTexture3D and ImageTexture3D have zero extraction logic — not populated into TextureRef** — grep of runtime/ for ComposedTexture3D, ImageTexture3D returns only generated_cpp_bindings/. MaterialSystem.hpp and SceneExtractor dispatch on Appearance.texture nodes; neither recognises X3DTexture3DNode subtypes. A scene using ComposedTexture3D or ImageTexture3D as Appearance.texture will yield no texture ref (white/fallback rendering) with no error.
  - ⚪ **No wiki subsystem page for Texturing3D and no coverage.md row** — ls docs/wiki/subsystems/ shows no texturing3d.md or texture-3d.md. grep of docs/wiki/coverage.md for 'Texturing3D' returns zero hits. The code comment at runtime/extract/TextureExtract.hpp:128 claims TextureTransform3D is 'documented' — that documentation does not exist in the wiki.
  - ⚪ **findings.yaml has zero entries for component Texturing3D despite multiple known gaps** — grep docs/conformance/findings.yaml for 'Texturing3D' returns zero hits. TXF-1/2/3 are tagged component: Texturing (§18, 2D). The §33 3D-specific behavioral gaps (repeatR, TextureTransform3D identity, 3D coord extraction, ComposedTexture3D/ImageTexture3D) have no deferred or open findings.

### Time

> TimeSensor lifecycle is fully and correctly implemented; the only active gaps are two tracked low-severity open findings (CONF-TDN1V, CONF-CRITIC-1) plus one new drift: the wiki's ctest -R target names for timesensor tests do not exist in CMakeLists.txt.

- **Spec** ✓ — ISO §8.2.4 (X3DTimeDependentNode) and §8.4.1 (TimeSensor) are well-covered by spec_rag. One node: TimeSensor (L1). All normative rules documented: activation/deactivation gating, loop=FALSE finishes current cycle, set_startTime ignored while active, pause/resume elapsed-time contract, fraction_changed boundary rule (f==0 && now>startTime → 1.0), cycleTime = startTime + N*cycleInterval, final time/fraction_changed guaranteed at deactivation. pauseTime_changed/resumeTime_changed must report simulation-now (CONF-TDN1V is the tracked deviation). One ambiguity: same-tick stop→start restart semantics are unverified (CONF-CRITIC-1).
- **Impl** ✓ — All key lifecycle paths are wired and verified by reading the source. X3DTimeDependentSystem.hpp:158-264 (advance()) implements the full state machine: activation at line 205, deactivation at line 191 and 242, pause/resume with timeBase shift at lines 302-325, non-looping completion with exact boundary time at lines 228-243 (TDN-3/TDN-7), set_startTime ignored via st.activeStartTime snapshot at line 164 (TDN-4), cycleTime = startTime+cycleIndex*cycleInterval at line 254 (RTC-3), fraction boundary rule at lines 360-367 (RTC-2), cycleInterval <= 0 guard at line 333 (RTC-1), final outputs before isActive=FALSE at lines 182-192 (RTC-4). TimeSensorSystem.hpp correctly emits time/fraction_changed (lines 52-57) and cycleTime (lines 59-64). Dynamic cycleInterval changes are honored (readCycleInterval() is called each tick). pauseTime_changed and resumeTime_changed are emitted as base-name events (lines 311, 325) which the field-alias resolver (X3DEventGraph.hpp:36-43) maps to _changed ROUTEs correctly. TimeSensorBehavior.hpp is legacy-only (fraction_changed-only, no lifecycle), retained for animation_test.
- **Wiki** ◑ — docs/wiki/subsystems/system-time.md is otherwise accurate — file paths, class names, State struct fields, seam points, and spec references all match the code. One concrete drift: the testing section (lines 79, 81, 85) documents three ctest -R target names that do not exist: 'x3d_event_timesensor', 'x3d_event_timesensor_rtc', and 'x3d_event_animation'. The actual CTest-registered name is 'x3d_events_tests' (CMakeLists.txt:1986, add_test NAME x3d_events_tests). Running 'ctest --preset dev -R x3d_event_timesensor' would select zero tests. See new_findings for detail.
- **Findings** — 2 open / 6 closed. Open: CONF-TDN1V (low) — pauseTime_changed/resumeTime_changed emit field-echo value not simulation-now per strict §8.2.4.4 reading; CONF-CRITIC-1 (low) — same-tick stop→start restart behaviour unverified. Closed/fixed (all code-verified): TDN-1 (pauseTime_changed emitted at pause edge, commit e92042e), TDN-2 (resumeTime_changed emitted at resume edge, e92042e), TDN-3 (loop=FALSE finishes current cycle, c7d2c21), TDN-4 (set_startTime ignored while active, c7d2c21), TDN-6 (strict resumeTime > pauseTime guard, 775c3ff), TDN-7 (final time at exact cycle boundary, 3b842d0). TDN-5 (AudioClip/MovieTexture time-lifecycle inert) is deferred and applies to the Sound component, not TimeSensor directly.

  **New findings (1):**
  - ⚪ **Wiki test command target names are stale — ctest -R selects zero tests** — docs/wiki/subsystems/system-time.md lines 79, 81, 85 document 'ctest --preset dev -R x3d_event_timesensor', 'ctest --preset dev -R x3d_event_timesensor_rtc', and 'ctest --preset dev -R x3d_event_animation' as the run commands. CMakeLists.txt:1947,1979-1986 shows both timesensor_test.cpp and timesensor_rtc_test.cpp and animation_test.cpp are compiled into a single executable with add_test(NAME x3d_events_tests ...). The CTest-registered name is 'x3d_events_tests'; the -R patterns in the wiki match nothing. Correct command: ctest --preset dev -R x3d_events_tests (or Catch2 filter: x3d_events_tests -tc timesensor_test).

### VolumeRendering

> All 13 VolumeRendering nodes exist only as generated bindings with zero behavioral runtime — no render output, no subsystem page, and no findings tracking the gap.

- **Spec** ✓ — ISO §41 (volume.md) is well-covered by spec_rag. All 13 nodes have normative prose, field tables, and spec section refs (§41.4.1–§41.4.13). Key rules confirmed: X3DVolumeDataNode sits at scene-graph peer level with X3DShapeNode (not inside Shape); default renderStyle is OpacityMapVolumeStyle; VolumeData.voxels accepts X3DTexture3DNode; IsoSurfaceVolumeData supports contour stepping via contourStepSize + surfaceValues; SegmentedVolumeData uses segmentIdentifiers + segmentEnabled for per-segment style dispatch. Spec is unambiguous for this component.
- **Impl** — — All 13 nodes exist as generated C++ bindings in generated_cpp_bindings/ (field reflection, accessors, visitor accept stubs) but there is zero behavioral runtime. Evidence: (1) grep across runtime/ for VolumeData/VolumeStyle/VolumeRender returns 0 hits (excluding unrelated ViewVolume/sound uses). (2) SceneExtractor.hpp:519 anchors render-item emission on hasField('geometry') — VolumeData nodes inherit X3DChildNode/X3DVolumeDataNode, not X3DShapeNode, so they have no 'geometry' field and fall through DFS as inert children. (3) MeshBuilder.hpp:1239 recognizedGeometryType() lists every supported geometry type; none of the VolumeRendering types appear. (4) No VolumeSystem, VolumeExtractor, or voxel-rendering path exists anywhere in runtime/. The nodes parse and survive in the scene graph but produce no render output whatsoever.
- **Wiki** ✗ — No subsystem page exists for VolumeRendering in docs/wiki/subsystems/ (ls confirmed). coverage.md (41 subsystem rows) has no VolumeRendering entry — the component is entirely absent from the wiki manifest. The generated conformance view docs/conformance/components/VolumeRendering.md shows all 13 nodes with Exists=check, Extract=dash, Behaves=dash, Findings=dash, which is accurate, but this is auto-generated, not a wiki subsystem page. There are no wiki claims to be wrong — the component simply has no wiki presence at all.
- **Findings** — 0 open / 0 closed. Zero findings in docs/conformance/findings.yaml for VolumeRendering (grep for 'VolumeRendering', 'VolumeData', 'VolumeStyle' all return 0 matches on the findings file). The complete absence of runtime behavior for 13 nodes across 3 levels is entirely untracked. No open, deferred, fixed, or closed findings exist for this component.

  **New findings (3):**
  - 🔴 **VolumeRendering component has zero behavioral runtime — all 13 nodes are inert stubs** — runtime/ grep for VolumeData/VolumeStyle returns 0 hits. SceneExtractor.hpp:519 anchors on hasField('geometry'); VolumeData/IsoSurfaceVolumeData/SegmentedVolumeData inherit X3DVolumeDataNode (no 'geometry' field) so they produce no RenderItem. MeshBuilder.hpp:1239 recognizedGeometryType() omits all VolumeRendering types. No VolumeSystem or voxel path exists. All 13 nodes: VolumeData, IsoSurfaceVolumeData, SegmentedVolumeData, OpacityMapVolumeStyle, BlendedVolumeStyle, BoundaryEnhancementVolumeStyle, CartoonVolumeStyle, ComposedVolumeStyle, EdgeEnhancementVolumeStyle, ProjectionVolumeStyle, ShadedVolumeStyle, SilhouetteEnhancementVolumeStyle, ToneMappedVolumeStyle — parse-only, behaviorally na.
  - 🟡 **No wiki subsystem page for VolumeRendering and no coverage.md row** — ls docs/wiki/subsystems/ confirms no volume page. docs/wiki/coverage.md lists 41 subsystem rows with no VolumeRendering entry. The component is absent from the wiki manifest entirely, violating the anti-drift discipline (CLAUDE.md §2: 'a [NEW FILE] with no citations probably needs a brand-new page').
  - 🟡 **Zero findings in findings.yaml for VolumeRendering despite complete behavioral absence** — grep -c 'VolumeRendering' docs/conformance/findings.yaml returns 0. The total behavioral gap (13 nodes, 3 levels, ISO §41) is untracked. Per CLAUDE.md, behavioral/spec-conformance gaps belong in findings.yaml; none have been filed for this component.
