---
title: x3d-cpp-gen Knowledge Wiki
summary: Start here — orientation to the project and links to every major knowledge area.
tags: [index, meta]
updated: 2026-06-20
related:
  - architecture.md
  - knowledge-map.md
  - coverage.md
  - seam-status.md
  - decisions/0001-ext-firewall.md
  - subsystems/cli-suite.md
  - guides/gate-system.md
---

# x3d-cpp-gen Knowledge Wiki

**Start here.** This is the single canonical, rendered knowledge home for the x3d-cpp project — a headless, renderer-agnostic X3D domain runtime SDK (renderers are consumers, not part of the SDK: the two in `examples/` are out-of-SDK consumers, firewalled behind their own build flags).

If you are new to the project, read this page first, then follow the links to the area you need.

## What this project is

x3d-cpp is a code-generation pipeline and runtime SDK for X3D. Two version axes
matter and are often confused: the **node layer** is generated from the X3D
**4.0** Unified Object Model, while the **parser** reads X3D **3.0–4.1**
encodings (XML, ClassicVRML, VRML97, JSON). So a 4.1 scene parses, but 4.1-only
nodes that the 4.0 UOM does not define (e.g. `EnvironmentLight`) are not in the
generated node layer — see `docs/sdk/v1-capabilities.md` (outside this wiki's
`docs_dir`, so it is not linked here).

- **Generator** (`src/x3d_cpp_gen/`) — reads the X3D Unified Object Model (XML spec) and emits spec-correct C++ node bindings via Jinja2 templates.
- **Generated bindings** (`generated_cpp_bindings/`) — committed, version-controlled C++ headers; regenerated by `mise run gen`.
- **Runtime** (`runtime/`) — the scene graph, event/behavior cascade, codec layer (XML/VRML/JSON readers+writers), extract pipeline (geometry/texture/topology), and the systems layer (Time, Interpolators, Navigation, Script/SAI, etc.).
- **SDK facade** (`include/x3d/sdk.hpp`) — the single public header consumers include.
- **CLI suite** (`tools/x3d-cli/`) — `x3d convert`, `x3d validate`, `x3d extract`, `x3d canonicalize`, `x3d sim`.

The runtime is designed to be consumed by renderers (the first real consumer is a VR CAVE) and other tooling; it has no rendering dependency itself.

## How to navigate this wiki

| Section | What it covers |
|---|---|
| [Architecture](architecture.md) | System map: layers, seams, data flow end-to-end |
| [Knowledge Map](knowledge-map.md) | The RAGs (spec_rag, code_rag): what each indexes and when to use which |
| [Coverage Manifest](coverage.md) | Full target set — every subsystem, decision, and guide; tracks what is covered vs. missing |
| [Seam-Status Matrix](seam-status.md) | The live genericity tracker — one row per renderer/engine seam; GREEN = interface frozen + ≥2 backends + a CI-gated swap-test |
| [Decisions](decisions/0001-ext-firewall.md) | ADRs — the binding "why" behind design choices |
| [Subsystems](subsystems/cli-suite.md) | One page per major functional area (purpose, key files, interfaces, tests) |
| [Guides](guides/gate-system.md) | Operational how-tos (build, gates, RAGs, add a command, workflows) |

## Status & coverage

All three build waves are complete: the [Architecture](architecture.md) spine, every subsystem and decision page, the operational guides, and the [Knowledge Map](knowledge-map.md). Completeness is tracked in the [Coverage Manifest](coverage.md) — the canonical target set (subsystems, ADRs, guides) with covered-vs-residual status. Any deferred area is named there rather than left implicit.

How work flows from a planning card to documented completion is described in `docs/contributor/card-to-done-workflow.md` (contributor material, outside this wiki). The product's core thesis — that every renderer/engine seam is *proven generic* only with a second independent backend + a CI-gated swap-test — is tracked live in the [Seam-Status Matrix](seam-status.md) (ScriptEngine is the first GREEN row; see [ADR-0022](decisions/0022-scriptengine-second-backend-swap-test.md)).

The **Subsystems** nav is grouped by architecture layer (Generator & Bindings → Runtime Core → Codecs & Parse → Event/Behavior Runtime → Extract → Boundaries & Consumers) so you can zoom from the [Architecture](architecture.md) map down to a single area. For semantic search across the codebase and the ISO spec, see the [Knowledge Map](knowledge-map.md).

## Key entry points outside the wiki

These systems are NOT duplicated here; the wiki links to them:

- **Dated specs/plans** (`docs/superpowers/specs/`, `docs/superpowers/plans/`) — the historical design record: one dated doc per feature/decision.
- **Conformance view** (`docs/conformance/INDEX.md`) — generated by `mise run conformance`; the behavioral conformance audit.
- **Deferral tracking** — split into the [GitHub Project](https://github.com/users/delta9000/projects/2) (engineering/planning deferrals) and `docs/conformance/findings.yaml` (behavioral/conformance gaps). The old `docs/superpowers/BACKLOG.md` is **deprecated** (stub points to both).
- **Auto-memory** (`~/.claude/projects/.../memory/MEMORY.md`) — agent session-persistence; durable facts graduate into this wiki in Wave 3.

## Conventions

Authors of wiki pages MUST follow [`_conventions/CONVENTIONS.md`](_conventions/CONVENTIONS.md). Templates:

- ADR: [`_conventions/adr-template.md`](_conventions/adr-template.md)
- Subsystem: [`_conventions/subsystem-template.md`](_conventions/subsystem-template.md)

The strict build gate (`mise run docs-build`) fails on broken internal links and nav orphans — run it before committing any wiki change.
